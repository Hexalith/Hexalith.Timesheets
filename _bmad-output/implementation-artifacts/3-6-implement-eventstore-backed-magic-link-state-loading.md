---
baseline_commit: 24a37c1c50c9c3b3504a03a7a939caf2720b45b0
---

# Story 3.6: Implement EventStore-Backed Magic-Link State Loading

Status: in-progress

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an external contributor,
I want valid magic links to load scoped confirmation state in the live host,
so that confirmation and adjustment work outside service-only tests without weakening no-disclosure behavior.

## Acceptance Criteria

1. **Given** a magic-link token is valid, unexpired, unused, tenant-scoped, and action-scoped, **when** the live host describes, confirms, or adjusts through the magic-link endpoints, **then** `IMagicLinkConfirmationCapabilityStateLoader` resolves the token hash, folds EventStore-backed capability state, folds scoped Time Entry state, and loads a fresh Activity Type catalog, **and** raw tokens, decoded material, tenant names, contributor details, target names, comments beyond policy, and failure reasons are not logged, projected, or returned.
2. **Given** capability or Time Entry events are duplicated, replayed, or rebuilt, **when** the loader folds state, **then** the resulting capability state, Time Entry state, and catalog trust state are deterministic and idempotent, **and** projection freshness or unavailable states are represented explicitly.
3. **Given** a token is malformed, unknown, expired, revoked, used, wrong-action, wrong-recipient, cross-tenant, replayed, or unresolved, **when** any magic-link endpoint is called, **then** the same no-disclosure external failure state is returned, **and** no Time Entry confirmation, adjustment, capability-use event, or protected detail is emitted.
4. **Given** the loader cannot access required EventStore state, Time Entry state, or Activity Type catalog state, **when** the request is evaluated, **then** the host fails closed with the same external no-disclosure response, **and** internal diagnostics record only correlation-safe outcome categories.

## Tasks / Subtasks

- [x] **Design and implement the token-hash → capability resolution index as a rebuildable, EventStore-backed lookup (AC: 1, 2, 3)**
  - [x] The external token path receives only the opaque `string` token (`t`); it carries no tenant/capability claim. Resolve the candidate capability by deriving the hash with the existing `IMagicLinkTokenGenerator.DeriveHash(token)` (SHA-256, Base64Url) and looking it up in a rebuildable EventStore-backed index that maps `MagicLinkTokenHash → (TenantReference, MagicLinkCapabilityId)`.
  - [x] Build the index only from `MagicLinkConfirmationCapabilityIssued` events (which carry `TokenHash`, `Tenant`, and `CapabilityId`). Implement it through the platform read-model path (`IDomainProjectionHandler` + `IReadModelStore`) or a Timesheets projection — never a mutable cache, static dictionary, SQL/Redis/Dapr-state table, local file, or Data Protection-only token as authority.
  - [x] Store only the token **hash** in the index, never the raw token or decoded capability material. The index is a candidate-resolver only.
  - [x] If no index entry matches the derived hash, fail closed (return `null`/unavailable) without distinguishing "unknown token" from any other failure.
- [x] **Implement the concrete EventStore-backed `IMagicLinkConfirmationCapabilityStateLoader` and replace the `Unavailable` default (AC: 1, 2, 3, 4)**
  - [x] Add a concrete loader (e.g. `EventStoreMagicLinkConfirmationCapabilityStateLoader`) under `src/Hexalith.Timesheets.Server/MagicLinks/` implementing all three interface methods. Keep the exact signatures: `LoadActivityTypeCatalogAsync`, `LoadCapabilityAsync(MagicLinkCapabilityId, …)`, `LoadTokenStateAsync(string oneTimeToken, …)`.
  - [x] `LoadCapabilityAsync`: fold the **authoritative** magic-link capability aggregate from EventStore events (`MagicLinkConfirmationCapabilityIssued/Used/Revoked/Expired`) into a `MagicLinkCapabilityState` using its existing `Apply(...)` overloads. Return `null` when the aggregate does not exist or cannot be resolved.
  - [x] `LoadTokenStateAsync`: derive hash → resolve candidate capability id + tenant via the index → fold capability state via `LoadCapabilityAsync` → fold the **scoped** `TimeEntryState` for `capabilityState.TimeEntryId`/`Tenant` from EventStore Time Entry events → load a fresh Activity Type catalog for that tenant. Bundle into `MagicLinkEndpointTokenState(capabilityState, timeEntryState, activityTypeCatalog)`.
  - [x] `LoadActivityTypeCatalogAsync`: load the tenant Activity Type catalog read model with explicit `ProjectionFreshnessMetadata`. Resolve the tenant the same way the existing write paths do (do not trust caller-supplied tenant; for the external token path the tenant comes from the resolved capability, not the request).
  - [x] **Single-use, revocation, and expiry validity authority comes only from the folded capability aggregate state**, never from the index or any projection read model (projections are non-authoritative for writes). The index resolves a candidate; folded aggregate state and the public `MagicLinkConfirmationCapability.IsValidForUse`/`IsValidForAdjustment`/`HandleUse` entry points decide validity.
  - [x] Register the concrete loader in `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs` (line 42) by replacing `UnavailableMagicLinkConfirmationCapabilityStateLoader`. Because registration uses `TryAddSingleton`, register the concrete type **before** the `Unavailable` default or remove the `Unavailable` line; do not leave both racing.
- [x] **Reuse the canonical EventStore fold pattern; do not hand-roll Dapr/actor access (AC: 1, 2)**
  - [x] Fold using the existing `State.Apply(@event)` idiom already present on `MagicLinkCapabilityState`, `TimeEntryState`, and `ActivityTypeCatalogState`. Order events by sequence number and de-duplicate by message id (idempotent, replay-safe, duplicate-tolerant), matching the existing projection folding in `MagicLinkConfirmationCapabilityProjection`/`TenantActivityTypeCatalogProjection`.
  - [x] Read events/state only through the Hexalith.EventStore client/SDK seam (`IReadModelStore` for persisted read models, the domain-service replay/projection path for folded aggregate state). Do not call Dapr state stores, brokers, or `IActorStateManager` directly from Timesheets, and do not invent stream-key formats — use the EventStore `AggregateIdentity` conventions with `DomainName = "timesheets"`.
  - [x] Construct aggregate identities with the same `(tenantId, domain, aggregateId)` shape used by the write path so reads fold exactly the streams the commands append to.
- [x] **Preserve and prove no-disclosure across every failure stage (AC: 1, 3, 4)**
  - [x] Every resolution stage that can fail — malformed/blank token, hash not in index, missing capability aggregate, terminal/expired/used/revoked capability, wrong action, wrong recipient/contributor, wrong target/Time Entry, cross-tenant mismatch, replayed token, missing Time Entry state, stale/unavailable catalog, unresolved EventStore access — must collapse to the **same** external outcome (`null` from `DescribeAsync`/`DescribeAdjustmentAsync`, or non-dispatch from `ConfirmAsync`/`AdjustAsync`), driving the single `Denied()` 403 with the shared `MagicLinkInvalidLinkDenial.Default` title/detail.
  - [x] Do not leak which stage failed through status code, headers, content type, response body, response length, timing-sensitive branches, log text, or metadata.
  - [x] Map internal failures to the existing `MagicLinkInvalidLinkOutcomeCategory` vocabulary for authorized internal diagnostics only; never surface the category externally. (Wiring categories into actual telemetry/audit remains optional per the v1 deferral — see Story 3.5 follow-up #3 — but the loader must produce them in a privacy-safe form if it records anything.)
  - [x] Log only correlation id, timestamp, safe outcome category, and hashed/scoped references where policy allows. Never log raw tokens, derived hashes where disallowed, decoded material, comments, command bodies, personal data, tenant/Party/Project/Work names, or EventStore envelopes.
- [x] **Confirm the internal admin paths also work in the live host (AC: 1, 4)**
  - [x] The issue (`POST /api/timesheets/magic-links/confirmation-capabilities`), revoke (`/{capabilityId}/revoke`), and expire (`/{capabilityId}/expire`) endpoints already call `LoadActivityTypeCatalogAsync` and/or `LoadCapabilityAsync`. Verify the concrete loader makes issue (null state for a new capability) and revoke/expire (existing folded state) work end-to-end, where today the `Unavailable` loader makes revoke/expire reject and issue run against an unavailable catalog.
- [x] **Add focused tests proving fold correctness, idempotency, and disclosure equivalence (AC: 1-4)**
  - [x] Loader unit/server tests: a valid candidate token resolves, folds capability state, folds the scoped Time Entry state, and returns a fresh catalog; duplicated/replayed/out-of-order events fold to the same deterministic state (idempotency); each invalid category fails closed returning unavailable/null **without revealing which stage failed**.
  - [x] Index tests: token-hash lookup resolves only via the rebuildable EventStore-backed projection; rebuild from `Issued` events reproduces the same mapping; the index stores no raw token and is non-authoritative for single-use/revocation/expiry.
  - [x] Endpoint/integration tests: with the concrete loader registered, GET/POST confirm and adjust routes return success for a valid token candidate and the identical opaque 403 for malformed, unknown, expired, used, revoked, wrong-action, wrong-recipient, cross-tenant, replayed, missing-Time-Entry, and stale-catalog cases. Prefer an HTTP-boundary host (`WebApplicationFactory`/`Mvc.Testing`) to also close Story 3.5 follow-up #2 if scope permits; otherwise keep service-level equivalence and note the deferral.
  - [x] Privacy/diagnostics tests: extend `DiagnosticsPrivacyTests` so logs, OpenAPI, read models, the new index, and metadata contain no raw token, disallowed hash, decoded material, comments, command bodies, personal data, target/display names, protected identifiers, or EventStore envelopes.
  - [x] Freshness tests: confirm `ConfirmAsync`/`AdjustAsync` fail closed when the Activity Type catalog freshness is not `Fresh` (Stale/Rebuilding/Unavailable/Degraded/Unknown).
- [x] **Verify affected build and test lanes (AC: 1-4)**
  - [x] `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet restore Hexalith.Timesheets.slnx -m:1 /nr:false`
  - [x] `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet build Hexalith.Timesheets.slnx --no-restore -warnaserror -m:1 /nr:false`
  - [x] Run affected tests individually: ArchitectureTests, Contracts.Tests, Server.Tests, Projections.Tests, and IntegrationTests.
  - [x] If `dotnet test` is blocked by local VSTest socket permissions (`SocketException (13): Permission denied`), use the README direct xUnit v3 executable fallback (`DOTNET_CLI_HOME=/tmp/dotnet-cli-home tests/<Project>/bin/Debug/net10.0/<Project>`) and record the reason in Debug Log References.

### Review Findings

Code review of `c2f68e5` (2026-09-13). Layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor.

**Decisions resolved (2026-09-14)**

- [x] [Review][Decision] Live host can never reach a `Fresh` Activity Type catalog, so valid magic links still fail closed — `LoadActivityTypeCatalogAsync` accepts only `Fresh`; `Merge(preserveFreshness: true)` yields `Fresh` only when the persisted catalog is already `Fresh`; only `FinalizeAsync` (shared rebuild) can first produce `Fresh`; and `/project/rebuild/shared/v1` is defined at `references/Hexalith.EventStore/.../EventStoreDomainServiceExtensions.cs:375` but called by nothing — no orchestrator, worker, gateway, or SDK test. This is the exact defect Story 3.6 was reopened to fix. Triage entry BH5 dismissed it by citing the AC's "supplies a Fresh catalog" wording, which does not answer how production reaches `Fresh`. Options: promote to `Fresh` in `ProjectAsync` once the folded cursor reaches stream head; trigger a rebuild at host startup; drive it from the platform orchestrator (platform change); or accept it and document an operator runbook step. Topology changes are forbidden by the spec and CLAUDE.md. **Resolved: option A** — promote the catalog to `Fresh` inside `ProjectAsync` once the folded cursor reaches stream head, so the host is self-healing with no operator step and no topology change. This relaxes the prior contract that only a deterministic full rebuild may claim `Fresh`; that relaxation is the accepted trade for Story 3.6 actually delivering working links.
- [x] [Review][Decision] EventStore domain-service routes are mapped without authorization on the externally reachable host — `app.UseEventStoreDomainService()` (`src/Hexalith.Timesheets/Program.cs:26`) maps `/process`, `/replay-state`, `/query`, `/project`, `/project/v2`, the rebuild routes and `/admin/operational-index-metadata`; none carry `RequireAuthorization()` and the host registers no authentication or authorization middleware, alongside anonymous magic-link routes. `tests/.../MagicLinkConfirmationHttpBoundaryTests.cs:792` proves it: an anonymous client POSTs `/project/v2` and gets `200 OK` while mutating both the token-hash index and the trust-bearing catalog read model. Mitigating: no Timesheets type derives from `EventStoreAggregate<>`/`EventStoreProjection<>`, so command/query routes dispatch nothing today; the projection-write routes are live. Options: `RequireAuthorization` on the SDK route group; bind them to an internal-only port; or rely on documented sidecar/network isolation. **Resolved: option C** — document sidecar/network isolation as the control in `docs/launch-readiness.md` and raise authorization of the SDK route group upstream. The Hexalith baseline forbids a domain module re-implementing hosting/DAPR plumbing, so the auth scheme belongs to the platform, not to this repo.
- [x] [Review][Decision] `Hexalith.Timesheets.Projections` now references `Hexalith.Timesheets.Server` and `Hexalith.EventStore.DomainService`, inverting layering and hollowing the kernel fitness rule — the projection assembly pulls in every command service and aggregate to reach four read-model/address shapes, and `Hexalith.EventStore.DomainService` carries `FrameworkReference Microsoft.AspNetCore.App` plus `Dapr.AspNetCore`, all on `ForbiddenKernelReferences`. `Kernel_projects_do_not_reference_runtime_hosting_ui_or_direct_persistence_packages` still passes only because it string-matches direct `Include` values, so the rule is satisfied in letter and broken in substance. Options: move the shared contracts to `Hexalith.Timesheets.Contracts` and drop the Server reference; or tighten the fitness rule to transitive closure and record an explicit waiver for Projections. **Resolved: option A** — move the four shared read-model/address shapes to `Hexalith.Timesheets.Contracts` and drop the `Projections`→`Server` reference, restoring the documented layering.
- [x] [Review][Decision] Gateway target is resolved from raw process environment variables at registration time — `ResolveDaprHttpEndpoint()` reads `DAPR_HTTP_ENDPOINT`/`DAPR_HTTP_PORT` and the registration reads `DAPR_API_TOKEN` via `Environment.GetEnvironmentVariable`, with the app id hardcoded as `"eventstore"`. Nothing is bindable via `IConfiguration`, overridable per environment, or testable without mutating the process — which is why `RuntimeRegistrationTests` swaps process-wide env vars (xUnit runs test classes in parallel, so this can leak across collections). A rotated `DAPR_API_TOKEN` is also never picked up after registration. Options: introduce an options/`IConfiguration` seam; or keep env-var resolution and isolate the tests in a non-parallel collection. **Resolved: option A** — introduce an `IConfiguration`/options seam for the gateway target, which also removes the process-environment mutation from `RuntimeRegistrationTests` and lets a rotated `DAPR_API_TOKEN` take effect.
- [x] [Review][Decision] `Hexalith.Timesheets.ServiceDefaults` is orphaned — `Program.cs` dropped `AddTimesheetsServiceDefaults()`/`MapTimesheetsDefaultEndpoints()`; both now have zero callers anywhere in `src/`, while the new fitness test locks the removal in with `program.ShouldNotContain("AddTimesheetsServiceDefaults")` and simultaneously asserts the host still references the project. Telemetry is not lost (the SDK's `AddServiceDefaults()` supplies OpenTelemetry and `MapDefaultEndpoints()` supplies health), so this is an ownership/scope question: CLAUDE.md says change `ServiceDefaults` only when a story names it, and neither the story nor the frozen spec names it. Options: delete the project and its reference; restore the call layered over the SDK defaults; or record the ownership handover in `docs/launch-readiness.md`. **Resolved: option C** — keep the project, record the ownership handover in `docs/launch-readiness.md`, and leave removal to a story that names `ServiceDefaults`, per CLAUDE.md.

**Patch**

- [ ] [Review][Patch] [From decision 1] Promote the tenant catalog to `Fresh` in `ProjectAsync` once the folded cursor reaches stream head, so a deployed host reaches `Fresh` without an operator-triggered shared rebuild [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:247]
- [ ] [Review][Patch] [From decision 2] Record sidecar/network isolation as the stated control for the unauthenticated SDK route group in `docs/launch-readiness.md` [docs/launch-readiness.md]
- [ ] [Review][Patch] [From decision 3] Move `MagicLinkTokenHashCapabilityIndexEntry`, `MagicLinkTokenHashCapabilityIndexReadModel`, `MagicLinkTokenHashCapabilityIndexProjection` and `MagicLinkActivityTypeCatalogReadModelAddress` to `Hexalith.Timesheets.Contracts`, then drop the `Projections`→`Server` project reference [src/Hexalith.Timesheets.Projections/Hexalith.Timesheets.Projections.csproj]
- [ ] [Review][Patch] [From decision 4] Bind the gateway target and API token through `IConfiguration`/options instead of `Environment.GetEnvironmentVariable` at registration time, and drop the env-var mutation from the registration tests [src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs:98]
- [ ] [Review][Patch] [From decision 5] Record the ServiceDefaults ownership handover to the EventStore SDK in `docs/launch-readiness.md` [docs/launch-readiness.md]

- [ ] [Review][Patch] Rebuild concurrency is chosen from ETag presence rather than value presence, so an existing key with a null/empty ETag commits `CreateOnly` and is rejected forever [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:150]
- [ ] [Review][Patch] Same defect in the index handler; `ReadModelEntry<TValue>(TValue? Value, string? ETag)` makes ETag nullable independently of Value [src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:139]
- [ ] [Review][Patch] No test anywhere asserts `ReadModelBatchOperation.Concurrency`; inverting both ternaries to always emit `CreateOnly` leaves every test green [tests/Hexalith.Timesheets.Projections.Tests/MagicLinkStateProjectionHandlerTests.cs:119]
- [ ] [Review][Patch] Loader's same-sequence equivalence check is weaker than `ProjectionEventReader.Equivalent` (type/format/payload only, omitting Timestamp/CorrelationId/MessageId/UserId/GlobalPosition), and only the group's first MessageId enters the uniqueness set, so a conflicting duplicate is silently dropped and its MessageId escapes the check — the BH6 fix landed on the projection path only [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:373]
- [ ] [Review][Patch] Both projection handlers are invisible to `GetHandlerDomainNames` (no `EventStoreDomainAttribute`, no parameterless constructor) and no Timesheets type derives from `EventStoreAggregate<>`/`EventStoreProjection<>`, so the host discovers zero domain names and `AddEventStoreDomainTelemetry` registers nothing; add `[EventStoreDomain(TimesheetsEventStoreIntegration.DomainName)]` to both [src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:13]
- [ ] [Review][Patch] HTTP `"cross-tenant"` case changed from a scripted mismatched-tenant capability to `null`, making it indistinguishable from `"unknown"`; endpoints now derive context from the resolved capability so `IsValidForUse` compares the tenant with itself, leaving the external cross-tenant defense resting on one loader comparison with a single unit test [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:896]
- [ ] [Review][Patch] No test delivers onto an already-`Fresh` catalog, so the branch that keeps magic links working between rebuilds is unobserved; dropping the `Fresh` arm leaves every test green [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:247]
- [ ] [Review][Patch] The valid-journey test manufactures the `Fresh` catalog it claims to prove — the fixture constructs `TenantActivityTypeCatalogProjectionHandler` by hand and calls `Store.Apply(plan)`, bypassing the mapped shared-rebuild route, and the `DirectIndexSeedCount` anti-seeding guard only counts index writes [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:695]
- [ ] [Review][Patch] `FinalizeAsync` publishes `candidateModel.ProjectionFreshness.Cursor ?? "0"` without maxing against the persisted cursor it overwrites, so a rebuild can move the freshness cursor backwards; the cursor also mixes `GlobalPosition` and `SequenceNumber` into one numeric space [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:145]
- [ ] [Review][Patch] Null-guard gaps that escape the handlers' `InvalidOperationException` catch as unhandled NREs: a JSON-null `EventTypeName` in `Matches<T>`, and a null `ProjectionFreshness` in `Merge`/`ParseCursor` [src/Hexalith.Timesheets.Projections/ProjectionEventReader.cs:74]
- [ ] [Review][Patch] The gateway guard is the only non-`TryAdd` registration in the method; a caller that pre-registers just `IEventStoreGatewayClient` skips options, the named `HttpClient` and `AddEventStoreDaprServiceInvocation` wholesale, and registration *after* `AddTimesheetsServerKernel()` is untested [src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs:47]
- [ ] [Review][Patch] Test read-model doubles ignore `storeName`, `Concurrency` and operation `Kind`, and `ScriptedReadModelStore` returns one global version as the ETag for every key, so store-name drift and per-key optimistic concurrency regressions cannot be caught [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:985]
- [ ] [Review][Patch] Dead code left by the refactor: `OrderedDistinct` can no longer filter anything (and its inverted blank-MessageId branch changed behavior with no test), the first `hasMore` assignment is dead, and `MagicLinkTokenHashCapabilityIndexProjection.Rebuild` has no production caller while its semantics changed from last-wins to omit-ambiguous [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:343]
- [ ] [Review][Patch] Consistency nits: the index handler declares its slot with the bare literal `"index"` while the catalog handler uses a named constant; `Microsoft.Extensions.Logging.Abstractions` is referenced but nothing in the project logs; both handlers omit the logger/context arguments `ReadModelWritePolicy` provides, so a `Retryable(DeliveryStateUnavailable)` outcome leaves no production trace [src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:26]

**Deferred**

- [x] [Review][Defer] Authorization for the EventStore domain-service route group (`/process`, `/query`, `/project/v2`, rebuild routes, `/admin/operational-index-metadata`) — deferred: per decision 2, the auth scheme belongs to the platform; the Hexalith baseline forbids a domain module re-implementing hosting/DAPR plumbing. Raise upstream against Hexalith.EventStore; this repo documents network isolation as the interim control.

- [x] [Review][Defer] A lagging replica reporting `LatestSequence` equal to the last returned sequence would let the loader fold a stale prefix as complete, so a Revoked/Used capability could fold as still Issued and the link stay reusable [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:301] — deferred: unverified; settled by whether `ReadStreamAsync` guarantees read-your-writes/linearizable reads against the writing actor. Would be high if confirmed.
- [x] [Review][Defer] The stream paging loop has no page or event cap, so a gateway returning `IsTruncated` pages indefinitely grows memory and hangs an external magic-link request [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:307] — deferred: not demonstrated reachable with a trusted gateway; a cap guards state never shown to occur.
- [x] [Review][Defer] Non-`Fresh` catalog states (`Stale`, `Rebuilding`, `Degraded`, `Unknown`) are collapsed to `Unavailable`, losing AC2's explicit freshness vocabulary [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:279] — deferred: resolution depends on the catalog-freshness decision above.
- [x] [Review][Defer] The valid-path acceptance is proven with `ITimesheetsAccessGuard` and `IEventStoreGatewayClient` substituted, so "the live host succeeds" is stronger than the evidence; production `DenyAll*` validators still deny [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:51] — deferred: the fail-closed defaults are intentional per CLAUDE.md and cannot change without a story that wires real validators.
- [x] [Review][Defer] `src/Hexalith.Timesheets.AppHost` declares no `eventstore` resource, so the default gateway target does not exist in the local topology — deferred: topology changes are forbidden by the spec's Never clause and CLAUDE.md.
- [x] [Review][Defer] The tenant-wide Activity Type catalog is persisted under the magic-link-scoped key `timesheets:magic-links:activity-type-catalog:{tenant}:v1` while the handler declares itself canonical writer for the whole tenant catalog, inviting a second divergent writer later — deferred: changing a persisted state key is a data migration, not a direct correction.
- [x] [Review][Defer] Story artifact and agent context are stale: Completion Notes still claim the index "has no live wiring" and that the loader folds the catalog from a domain-wide read (both now false), the File List omits all eight new files, and CLAUDE.md's "Known pitfalls" entry about the unwired index is obsolete — deferred: the fix edits spec and agent-context files.

**Rejected**

- `false` — `ProjectionEventReader` hard-coding `expectedSequence = 1` breaks incremental delivery: `/project/v2` delivers full aggregate history (`GetEventsAsync(0)`) and 1-based contiguity matches `EventPersister` sequence allocation, so no incremental window exists.
- `false` — Two non-identical `Issued` events terminally fail the delivery: an aggregate emitting two distinct issuances violates a domain invariant never shown reachable; failing closed there is correct behavior, and the analogous BH8 was already rejected.
- `false` — Dropping `AddTimesheetsServiceDefaults` loses OpenTelemetry: the SDK's `AddEventStoreDomainService` calls `AddServiceDefaults()` → `ConfigureOpenTelemetry()` (logging, metrics, OTLP exporter) and `UseEventStoreDomainService` calls `MapDefaultEndpoints()` for health. The orphaned-project half survives as a decision item.
- `false` — `MatchesTimeEntryIdentity` omitting a tenant check on `TimeEntryRecorded` is a cross-tenant hole: the record carries no `Tenant` member at all, and the stream is already tenant-scoped by `ReadAllEventsAsync(tenant, …)`.
- `false` — Story `Status: done` contradicts `sprint-status.yaml: review`: this is the workflow's transient state, synced during the presentation step; the proposed fix edits the spec under review.
- `low` — `ProtectionMetadata` is not checked before JSON parsing: the consequence is a fail-closed opaque denial rather than disclosure, and the fix guards a state never shown reachable.
- `low` — An empty or fully unrecognized `Events` collection is acknowledged as `AlreadyCompleted`: by design for capability streams carrying no issuance event, and no truncated-delivery path was demonstrated.

### Review Findings — live-host catalog freshness increment (2026-09-14)

Code review of `eb11472..f38214d`. Layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor. Triage re-verified every layer claim against the working tree. A large share of the raw findings were already recorded in `deferred-work.md` by the previous round; those were re-verified as still accurate and stay deferred rather than being re-filed.

**Decision needed**

- [x] [Review][Decision] **Resolved 2026-09-14 — option 4 (keep the gate, make the category honest).** Severity corrected from high to medium during resolution: `DescribeAsync` already denies on a non-`Fresh` catalog via `TryResolveDisplayLabel` (`MagicLinkConfirmationCapabilityCommandService.cs:510`), and the command service is unchanged in this increment, so the availability consequence is pre-existing, not introduced here. The gate closes a real direct-`POST` bypass and stays. Applied: `MagicLinkConfirmationCapabilityEndpoints.cs:178-189` now reports `Unknown` when `state.CapabilityState is null` and `StaleCatalog` only behind a resolved capability; the obsolete `"stale-catalog" when route.Name == "confirm-submit" => null` fixture workaround was removed; a scripted `"unresolved"` case (null capability + unavailable catalog) now mirrors the concrete loader's `UnavailableTokenState()`, and `Confirm_submit_reports_stale_catalog_only_when_a_capability_resolved` pins both arms — verified failing when the fix is reverted. Left open by choice: the host-layer placement (option 3 would move it to the server layer but discards the `StaleCatalog` category), the timing asymmetry, and the access-guard skip on already-denied requests. Original finding: the confirm/submit freshness gate denies valid confirms on state `ConfirmAsync` never reads, and relabels every loader failure as `StaleCatalog` — `src/Hexalith.Timesheets/Endpoints/MagicLinks/MagicLinkConfirmationCapabilityEndpoints.cs:178-185` (added in `f38214d`) returns before `service.ConfirmAsync`. Three consequences, one root cause. (a) `ConfirmAsync` takes no `ActivityTypeCatalogReadModel` (`MagicLinkConfirmationCapabilityCommandService.cs:125-132`) — confirm never consults the catalog, yet the gate blocks it. On a host deployed against an existing tenant the catalog read model does not exist until a new Activity Type event is delivered over `/project/v2`, and `/project/rebuild/shared/v1` still has no caller — so every magic-link confirm is denied for that whole window. This is the defect Story 3.6 was reopened to remove, reintroduced for the confirm route. (b) The concrete loader returns `UnavailableTokenState() => new(null, null, UnavailableCatalog())` for blank token, hash-not-in-index, missing capability, hash mismatch, cross-tenant and missing-Time-Entry (`EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:61-110`), so all of them now log `StaleCatalog` instead of `Unknown` and never reach `_accessGuard.AuthorizeAsync`. External responses stay identical; internal diagnostics now point operators at a projection problem for what is an unknown token. (c) The same rule lives at two layers with two outcomes: confirm gates at the host endpoint, adjust gates in the server layer at `MagicLinkConfirmationCapabilityCommandService.cs:241`. `epic-3-context.md` puts validation orchestration in the server layer, and `MagicLinkConfirmationCapabilityEndpoints.cs` appears in neither the frozen sub-spec's Code Map nor its three (all `[x]`) Execution tasks. Also carried by this entry: the denial short-circuits before `DeriveHash` and authorization, making a non-`Fresh` denial measurably shorter on the same route; and `MagicLinkConfirmationHttpBoundaryTests.cs:986` still maps `"stale-catalog" when route.Name == "confirm-submit" => null`, a pre-gate workaround that now makes the denial unattributable. Options: keep the gate as the frozen matrix row reads literally; narrow it to the routes that consume the catalog (adjust/describe) and leave confirm to the server layer; or move it into the server layer for both mutating routes. The frozen triage entry RVG4 chose the endpoint gate deliberately, so narrowing it renegotiates a human-approved decision.
- [x] [Review][Decision] **Resolved 2026-09-14 — option 2 (update the readiness doc to `13.5.1-beta.752`).** `docs/launch-readiness.md:22,58` now match the submodule catalog; `LaunchReadinessTests` passes and the architecture lane is 54/54 (was 53/54). The frozen sub-spec's "leave `docs/launch-readiness.md` alone" boundary is knowingly set aside because patch item P5 requires editing the same file anyway; reverting a pushed platform pin to satisfy a documentation assertion would fix the wrong side. Note `19fc5b8` is already on `origin/main`. Original finding: a `references/Hexalith.Builds` gitlink bump carried in this increment breaks `LaunchReadinessTests`, and the sub-spec records the failure as pre-existing — commit `19fc5b8` (inside the reviewed range) moves `references/Hexalith.Builds` from `bf8adb7` to `aee0132`, changing `Props/Directory.Packages.props:136` `CommunityToolkit.Aspire.Hosting.Dapr` from `13.5.1-beta.751` to `13.5.1-beta.752`. `docs/launch-readiness.md:22,58` still says `751`, and `LaunchReadinessTests.cs:168,186` asserts the readiness doc matches the catalog — so the architecture lane's one failure was introduced here, not inherited. The sub-spec's Verification section calls it "the pre-existing package-currency mismatch". CLAUDE.md also forbids staging `references/` gitlinks a change does not own. Options: revert the gitlink to `bf8adb7`; update `docs/launch-readiness.md` to `752` (the sub-spec's Boundaries say to leave that document alone); or accept it and correct the Verification record to say this increment introduced it.

**Patch**

- [ ] [Review][Patch] Malformed wire input escapes both projection handlers' fail-closed catch as an unhandled exception [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:53-60,173] — `ProjectAsync` catches only `InvalidOperationException`, but `ProjectionRequest.Events` is a plain non-`required` array on a wire DTO, so `"events": null` reaches `ProjectionEventReader.Normalize` and throws `ArgumentNullException`; an `ActivityTypeCreated` payload missing `activityTypeId` leaves the property null and `ActivityTypeIdFor(payload).Value` throws `NullReferenceException`; a present-but-blank `activityTypeId` throws `ArgumentException` out of `ActivityTypeId`'s validating constructor, which `ProjectionEventReader.Deserialize` does not catch (it catches `JsonException` only). The index handler has the same three holes at `MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:45-56,179` for null `Tenant`/`CapabilityId`/`TokenHash`. The frozen I/O matrix requires `Failed(DeliveryIdentityConflict)` and no write; the routes are mapped anonymously by `UseEventStoreDomainService()`. Distinct from the already-deferred null-guard item, which names only `Matches<T>`'s JSON-null `EventTypeName` and a null `ProjectionFreshness`.
- [ ] [Review][Patch] The loader's new stream-integrity guards are unfalsifiable — no test can fail them [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:324-339,373-380] — the page-scope rejection compares `page.Tenant`/`page.Domain`/`page.AggregateId` against the request, but `ScriptedGatewayClient` (Server.Tests) and `ScriptedEventStoreGateway` (IntegrationTests) both echo the request values straight back, and the `WithPageTransform` hook is used only to mutate `Metadata` — deleting all three checks leaves every test green. The `"The EventStore stream history is ambiguous."` throw has no test at all: the one same-sequence duplicate case feeds byte-identical events, so the equality arm never trips, and `grep -n "ambiguous"` on the loader suite returns nothing. Removing it lets a gateway returning `Issued` and `Used` both at sequence 2 fold a used capability as still usable.
- [ ] [Review][Patch] The catalog tenant-scope and uniqueness gate lost its only test [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:279-291] — `LoadActivityTypeCatalogAsync` replaced its own fold, which excluded project-scoped types structurally, with a persisted read-model read plus a shape gate. The deleted `LoadActivityTypeCatalogAsync_folds_only_tenant_scoped_items_and_reflects_rename_and_deactivation` was the only assertion of that guarantee; the three replacement tests supply only a tenant-scoped happy path, a non-`Fresh` catalog and a blank label. Dropping `item.Scope != ActivityTypeScope.Tenant || item.Project is not null` and the duplicate-id `GroupBy` leaves every suite green, while `MagicLinkConfirmationCapabilityCommandService.TryResolveActivityTypeScope` propagates `selected.Scope` into the external adjust. Helpers `ProjectScopedActivityCreated()` and `SecondActivityCreated()` are now unreferenced.
- [ ] [Review][Patch] Store and transport failures on the projection write path never reach `Retryable(DeliveryStateUnavailable)` [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:78-81] — the catch is `InvalidOperationException` only. `ReadModelWritePolicy.UpdateAsync` throws that on retry exhaustion, but a Dapr/HTTP/JSON failure out of `GetAsync` or `TrySaveAsync` escapes the handler entirely, so a transient store outage is reported as a handler failure rather than the retryable outcome the code declares. Same at `MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:77-80`.
- [ ] [Review][Patch] `docs/launch-readiness.md` still waives magic-link live end-to-end resolution as unwired [docs/launch-readiness.md:78] — the row reads "the token-hash candidate index has no projection-host wiring and resolves empty in the running topology", with the revisit condition "Projection host populates `MagicLinkTokenHashCapabilityIndexProjection`". `Program.cs:15-17` now passes `typeof(TimesheetsProjectionsMarker).Assembly` to `AddEventStoreDomainService`, and `MagicLinkTokenHashCapabilityIndexProjectionHandler` is the canonical writer. The overall CONCERNS decision line lists the same item among the waived reasons. Separate from the already-deferred launch-readiness goal, which covers only review decisions 2 and 5.

**Deferred**

- [x] [Review][Defer] Eight prior-round goals re-verified against the current tree and still accurate [_bmad-output/implementation-artifacts/deferred-work.md] — deferred: pre-existing ledger entries, not re-filed. ETag-presence-instead-of-value concurrency selection in both `FinalizeAsync` methods plus the total absence of any `ReadModelBatchOperation.Concurrency`/`StoreName`/`Kind` assertion (inverting both ternaries leaves the suite green); the loader's same-sequence equivalence check being weaker than `ProjectionEventReader.Equivalent` with only the first `MessageId` entering the uniqueness set; the `Matches<T>` JSON-null `EventTypeName` and null `ProjectionFreshness` null-guard gaps; the missing `[EventStoreDomain]` attribute on both handlers, which leaves `GetHandlerDomainNames` discovering nothing (no Timesheets type derives from `EventStoreAggregate<>`/`EventStoreProjection<>` either) so `AddEventStoreDomainTelemetry` registers no domain; dead code left by the refactor (`OrderedDistinct` can no longer filter anything now that `ReadAllEventsAsync` normalizes, the first `hasMore` assignment, the uncalled `MagicLinkTokenHashCapabilityIndexProjection.Rebuild`, eleven fixtures still scripting the removed domain-wide stream); the bare `"index"` slot literal, unused logging reference and omitted `ReadModelWritePolicy` logger arguments; HTTP cross-tenant coverage, now byte-identical to `"unknown"` because `MagicLinkExternalRequestContext.FromResolvedCapability` derives the request tenant from the capability itself, leaving the defense on one loader comparison with a single unit test; and gateway endpoint/token binding through `IConfiguration` instead of `Environment.GetEnvironmentVariable`, whose test fixtures mutate process-wide env vars under parallel xUnit collections.
- [x] [Review][Defer] The kernel-reference fitness rule no longer observes what `Projections` pulls in transitively [tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/DependencyDirectionTests.cs:111-130] — deferred: `Kernel_projects_do_not_reference_runtime_hosting_ui_or_direct_persistence_packages` reads only direct `Include` attribute values, so the new `Hexalith.EventStore.DomainService` reference (which carries `FrameworkReference Microsoft.AspNetCore.App` and `PackageReference Dapr.AspNetCore`) enters the Projections closure with the rule still green. Ledger decision 3 removes the leak by moving the four shared shapes to `Hexalith.Timesheets.Contracts` and dropping the `Projections`→`Server` reference, which is the better fix than re-pointing the rule.
- [x] [Review][Defer] The "Known pitfalls" entry in the three agent entry points is now false [CLAUDE.md:105] — deferred: the fix edits agent-context files. `CLAUDE.md`, `AGENTS.md` and `.github/copilot-instructions.md` all still read "`MagicLinkTokenHashCapabilityIndexProjection` has no projection-host wiring; valid links fail closed until a story wires it". `Program.cs` now discovers the projections assembly and the index handler is the canonical writer, so the entry misdirects the next agent. CLAUDE.md requires the three to be kept synchronized as normalized text.

**Rejected**

- `false` — `NextContinuationToken` is ignored, so a truncated prefix folds as a complete history: `hasMore = page.Metadata.IsTruncated || fromSequence < latestSequence` continues whenever more events exist, and `LatestSequence` is itself pinned by the page-scope validation. The claimed outcome needs a dishonest `LatestSequence`, not an unread continuation token.
- `false` — `AccumulateAsync` fold failures are unguarded and should return a handler result: `AccumulateAsync` returns a rebuild candidate, not a `DomainProjectionHandlerResult`; there is no `Failed` shape available to it, so throwing is the only abort signal and matches the deliberate `throw` already at `:109`.
- `false` — `Timesheets_host_delegates_shared_defaults_to_the_eventstore_sdk_once` pins an orphaned `ServiceDefaults` reference: CLAUDE.md requires `Hexalith.Timesheets.ServiceDefaults` be kept and changed only by a story that names it, so the retained reference is documented policy, and the SDK's `MapDefaultEndpoints` is a superset of the removed development-only `/health`.
- `low` — a project-scoped `ActivityTypeCreated` folds to zero items yet promotes the catalog to `Fresh`: the frozen decision defines `Fresh` as "every aggregate this host has been delivered, each folded from creation" rather than tenant-complete, which a project-scoped aggregate satisfies, and every downstream consumer fails closed on the empty item set. The fix adds a guard for a benign outcome.
- `low` — a malformed persisted cursor parses to `0` and can lower the published cursor: cursors are `long`s written only by this handler, the loader gate never reads them, and the fix adds a throw on a state never shown reachable.
- `low` — the anti-seeding counters intercept only `SaveAsync`, not `TrySaveAsync`: they do catch the direct-seeding pattern they were written for, and closing the hole means distinguishing production projection writes from fixture writes on the same method.
- `low` — sequence gaps and payload-parse failures both report `DeliveryIdentityConflict` while `DeliveryGap` exists: separating them requires distinct exception types threaded through the whole fold path, which is more than a direct correction.
- `low` — an unrecognized Activity Type event is skipped before promotion: no current producer emits one, and the frozen triage already rejected this reasoning as conflicting with additive consumer tolerance.
- `low` — `promoteCompleteLiveHistory` names a completeness property `Merge` never verifies: both callers are private and in-file, the proof genuinely lives upstream in `Normalize` and `FoldAggregate`, and the named harm is a hypothetical future caller.
- rejected per rule — story bookkeeping contradicts the code (File List omits all eight new files, Completion Notes still describe the deleted domain-wide catalog read and a confirm path that ignores the catalog, Debug Log counts disagree with the sub-spec's verified totals, Change Log ends 2026-06-22, `Status: in-progress` versus `sprint-status.yaml: review`, 19 items still listed as open Patch): the fix edits the spec under review, and status is synced by this workflow's own final step.


### Review Findings — projection and loader boundary increment (2026-09-14)

Scope reviewed: `c2f68e5^..HEAD` (40 files, +3526/-217). Four layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor. The story's declared `baseline_commit: 24a37c1c` was not used — it is ~4 months stale and spans epics 4-5, the BMAD 6.12 upgrade and the `references/` relocation.

**Decisions needed**

- [x] [Review][Decision] Live catalog delivery promotes the whole tenant catalog to `Fresh` from one aggregate's history — `Merge(..., promoteCompleteLiveHistory: true)` is passed unconditionally by `ProjectAsync`, but "complete live history" is a property of the *single aggregate* being delivered, not of the tenant catalog. `Catalog_complete_live_delivery_promotes_an_existing_stale_catalog` pins the bad outcome directly: a catalog persisted as `Stale` is promoted to `Fresh` by one unrelated sibling `ActivityTypeCreated`. The loader gates solely on `Fresh`, so an incomplete catalog is served as authoritative — the exact invariant this increment claims to harden. Second path to the same defect: a project-scoped `ActivityTypeCreated` folds to zero items (`TenantActivityTypeCatalogProjection.Apply` admits only `{ Scope: Tenant, Project: null }`), passes `FoldAggregate`'s `Items.Count > 1` check vacuously, and publishes an empty `Fresh` catalog — after which `ConfirmAsync` (which takes no catalog) succeeds while `DescribeAsync` 403s for want of a label. Note for triage history: round 2 rejected a neighbouring `low` on the grounds that the harm was "a hypothetical future caller"; this entry is a demonstrated outcome with a test that encodes it, not that claim. Options: (a) never raise freshness on a live single-aggregate delivery — preserve the persisted state, leaving `Fresh` to `FinalizeAsync` alone; (b) keep promotion but require the persisted catalog to already be `Fresh`; (c) accept and document that live deliveries define tenant freshness. [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:74,245-258] **Resolved (a, narrowed):** `Merge` no longer raises freshness on a live single-aggregate delivery — an absent or already-`Fresh` catalog stays `Fresh`, but a catalog the writer marked `Stale`/`Rebuilding`/`Degraded`/`Unknown` is preserved; only `FinalizeAsync` promotes those. `FoldAggregate` now returns `null` for a delivery that folds to no tenant-catalog item, so a project-scoped Activity Type no longer publishes an empty `Fresh` catalog. `Catalog_complete_live_delivery_promotes_an_existing_stale_catalog` is renamed and inverted to pin the corrected behaviour.
- [x] [Review][Decision] The host now maps the EventStore SDK's unauthenticated write surface on the internet-facing magic-link host — `UseEventStoreDomainService()` maps `POST /process`, `/replay-state`, `/query`, `/project`, `/project/v2`, `/project/v2/reconcile`, `/project/rebuild/{v1,stage,commit,abort,verify}`, `/project/rebuild/shared/v1` and `/admin/operational-index-metadata`, none carrying `RequireAuthorization` (`EventStoreDomainServiceExtensions.cs:210-300`), on the same host that deliberately serves anonymous magic-link routes. `DispatchProjectionAsync` in the new integration test POSTs anonymously to `/project/v2` and asserts `200 OK` while mutating both the cross-tenant token-hash index and the trust-bearing catalog. Same root cause, two further effects: `MapDefaultEndpoints()` now maps `/health`, `/alive`, `/ready` unconditionally where `MapTimesheetsDefaultEndpoints` gated `/health` behind `IsDevelopment()` (`ServiceDefaults/Extensions.cs:64-67`); and `Hexalith.Timesheets.ServiceDefaults` is now referenced but never called, against the CLAUDE.md rule that it changes only when a story names it. No story in this increment names it. Options: (a) record sidecar/network isolation as the stated control and document it (this is the still-unfulfilled Decision 2 patch item); (b) bind the SDK routes to a separate non-public port; (c) put the SDK route group behind authorization. [src/Hexalith.Timesheets/Program.cs:15,26] **Resolved (b), after the user authorized AppHost changes.** The control is now enforced, not documented: `InternalSurfaceGuard` runs ahead of routing and refuses the nine domain-service path prefixes on any port but the configured internal one, answering `404` rather than `403` so a refusal does not confirm the route exists. It is fail-closed — unset means refused everywhere, so a deployment that forgets the split loses projection delivery instead of publishing a write surface. `Hexalith.Timesheets.AppHost` now declares the Timesheets project with a public `http` endpoint and a non-proxied `internal` endpoint on 8081, and passes that port to the host, which is what makes it a wired control rather than an assumption. `InternalSurfaceGuardTests` proves all nine routes are refused unconfigured while the magic-link and metadata routes stay reachable; the HTTP-boundary factory opts in via `AllowOnAnyPort` because `TestServer` reports `LocalPort` as `0`. Residual risk, the `/health` un-gating, and the DAPR access-control revisit condition are recorded in `docs/launch-readiness.md`. **Consequence to note:** AppHost now launches the Timesheets host, which has no EventStore resource to reach yet, so under `aspire start` it comes up and fails closed. **Verified live on 2026-09-15:** the first `aspire start` exposed a real defect in the AppHost change — with no `launchSettings.json`, `AddProject` contributes no endpoint of its own, so declaring only `internal` left the magic-link routes unpublished entirely. Both endpoints are now declared explicitly. On the re-run every resource reported `Healthy` and `aspire stop` completed cleanly; on `:8080` all six domain-service routes answered `404` while `/metadata/timesheets` answered `200` and magic-link confirm answered `403`, and on `:8081` `/admin/operational-index-metadata` answered `200` and `/project/v2` answered `400`. The `400` also confirms in a live host the triage refutation above: the SDK validates the dispatch envelope and returns `BadRequest` rather than letting an `ArgumentException` escape.
- [x] [Review][Decision] `StaleCatalog` is unreachable in the live host, and the freshness gate sits outside the authorization path — `LoadTokenStateAsync` returns `UnavailableTokenState()` (capability `null`) on every non-`Fresh` catalog, so the endpoint's `state.CapabilityState is null ? Unknown : StaleCatalog` always yields `Unknown` with the concrete loader; the `StaleCatalog` arm is reachable only by `ScriptedMagicLinkStateLoader`. A genuinely stale projection behind a valid token is therefore logged as `Unknown` — the operator confusion the round-2 decision ("keep the gate, make the category honest") was applied to remove. The same gate also returns before `DeriveHash` and `_accessGuard.AuthorizeAsync` — the new test asserts `AuthorizationCount` is unchanged — leaving a non-`Fresh` denial measurably shorter on a route whose whole purpose is response indistinguishability, and leaving no authorization-audit record. Options: (a) have the loader carry a resolved capability alongside an explicitly non-`Fresh` catalog so the category is honest; (b) drop the `StaleCatalog` arm and the enum member; (c) move the gate inside the command service so hashing and authorization still run. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:106-112, src/Hexalith.Timesheets/Endpoints/MagicLinks/MagicLinkConfirmationCapabilityEndpoints.cs:178-189] **Resolved (b), after (a) was tried and rejected by the tests:** carrying the capability through a non-`Fresh` catalog broke four `ShouldBeOpaqueFailClosed` tests, correctly — `LoadActivityTypeCatalogAsync` collapses stale, rebuilding, degraded, absent, unreadable and invalid-shape into one `Unavailable` state, so nothing can distinguish a lagging projection from an unreadable one. The endpoint now reports `Unknown` unconditionally with a comment naming the deferred AC2 work as the prerequisite for an honest `StaleCatalog`; the loader keeps discarding the bundle. `Confirm_submit_reports_stale_catalog_only_when_a_capability_resolved` is replaced by `Confirm_submit_reports_one_indistinguishable_category_for_every_non_fresh_catalog`. The gate stays at the endpoint because `ConfirmAsync` takes no catalog; the hashing/authorization skip is unchanged and remains open.
- [x] [Review][Decision] Two new architecture assertions pin structures the ledger already resolved to remove — `Timesheets_host_discovers_projection_handlers_and_maps_canonical_sdk_routes` asserts `projectionsProject.ShouldContain("Hexalith.Timesheets.Server")`, cementing the `Projections` → `Server` layering inversion that Decision 3 resolved (option A: move the four shapes to `Contracts` and drop the reference); `Timesheets_host_delegates_shared_defaults_to_the_eventstore_sdk_once` asserts the host still references `ServiceDefaults` while never calling it, cementing the orphan. The kernel rule stays green only because `ForbiddenKernelReferences` string-matches package names, which a `ProjectReference` path never matches — so the transitive `Microsoft.AspNetCore.App` / `Dapr.AspNetCore` arrival into a kernel project is invisible. Both follow-ups now have to delete assertions added here. Options: (a) do the `Contracts` move now; (b) keep the inversion but drop the pinning assertions; (c) accept and re-scope the decisions. [tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/DependencyDirectionTests.cs] **Resolved (b):** both pinning assertions removed, each replaced by a comment naming the decision that owns the structure. The structures themselves are untouched, so decisions 3 and 5 no longer have to begin by deleting a test.
- [x] [Review][Decision] The magic-link live end-to-end launch gate moved from `waived` to `implemented` on stubbed evidence — `docs/launch-readiness.md` flips the row and drops it from the overall CONCERNS list, with Risk and Revisit blanked to `-`. The proving journey runs under `MagicLinkHttpBoundaryFactory(useConcreteLoader: true)`, which replaces `IEventStoreGatewayClient` with `ScriptedEventStoreGateway`, `IReadModelStore` with an in-process store, and `ITimesheetsAccessGuard` with `ScriptedAccessGuard`; `Hexalith.Timesheets.AppHost` still declares no `eventstore` resource, and `deferred-work.md` in this same diff still records both facts as open. Options: (a) restore the waiver; (b) keep `implemented` and state the in-process-stub caveat in the Risk column. [docs/launch-readiness.md] **Resolved (a+b):** the row is reclassified `implemented / waived`, states that the proving journey substitutes `IEventStoreGatewayClient`, `IReadModelStore` and `ITimesheetsAccessGuard` and that AppHost declares no `eventstore` resource, and carries a Risk and a Revisit condition again. Magic-link live-topology resolution is restored to the overall CONCERNS list.
- [x] [Review][Decision] Six `references/` gitlinks are staged that this change does not own — `Hexalith.Builds`, `Conversations`, `EventStore`, `FrontComposer`, `Tenants`, `Works`, against the CLAUDE.md pitfall. The `Builds` bump (`CommunityToolkit.Aspire.Hosting.Dapr` 13.5.1-beta.751 → 752) is the sole reason two `docs/launch-readiness.md` package rows had to change here, so an unrelated pointer drove a documentation edit inside a frozen sub-spec that said to leave that file alone. Options: (a) unstage the gitlinks and revert the package rows; (b) keep them and record the ownership explicitly. [references/] **Resolved (b), and escalated:** the gitlinks are already in committed history, and CLAUDE.md forbids reverting them without a task that owns the pointer, so they stand. This review found the bump was not merely cosmetic — the `Hexalith.EventStore` pointer added `IEventStoreGatewayClient.GetCommandStatusAsync`, which **left the solution unbuildable at `HEAD`**: `ScriptedGatewayClient` and `ScriptedEventStoreGateway` did not implement it. Both doubles now do, and the build is green. The story's claim that the build and test lanes were verified could not have held at this commit.

**Patches**

- [x] [Review][Patch] Anti-seeding guard and read-model doubles cannot observe what they assert — `DirectIndexSeedCount`/`DirectCatalogSeedCount` increment only in `SaveAsync`, but every handler write goes through `TrySaveAsync` via `ReadModelWritePolicy.UpdateAsync`, so `ShouldBe(0)` passes by construction and would not catch the direct seed it was added to catch. `InMemoryReadModelStore.GetAsync` dispatches on `typeof(TValue)` alone, ignoring `storeName` and `key`, so the loader's tenant-scoped catalog key is never verified and a handler writing to the wrong Dapr component passes; `ScriptedReadModelStore` ignores its `etag` argument and returns one global version for every key. [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:1140-1160, tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:963-975] **Applied:** `ProjectionBackedReadModelStore.SaveAsync` now throws for both read-model types instead of counting, so direct seeding is impossible rather than merely asserted-zero-by-construction; `InMemoryReadModelStore.GetAsync` asserts the store name and the tenant-scoped key for both the catalog and the index.
- [x] [Review][Patch] Four new fail-closed guards ship with no falsifying test — each can be deleted or inverted with every existing test still green: `ReplaceTenant`'s cross-tenant token-hash collision removal (the one test's replacement key never collides); `ToLoaderVisibleIndex`'s foreign-tenant rejection (every test's candidate comes from `AccumulateAsync`, which cannot carry one); `ResolveCandidateAsync`'s null/blank index-entry guard (every test builds the index through `IndexWith`, which never produces a degenerate entry — a blank tenant id would otherwise drive an unscoped stream read); and the `DAPR_HTTP_PORT` branch, whose only test expects `http://localhost:3500`, byte-identical to the hard-coded fallback. [src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkTokenHashCapabilityIndexProjection.cs:82-85, src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:215-223, src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:130-135, src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs:111-115] **Applied for three; one sub-claim refuted.** Added `Index_rebuild_drops_a_hash_another_tenant_already_owns`, `Index_rebuild_rejects_a_candidate_carrying_another_tenants_entry` (raw-bytes candidate, since `IndexRebuildCandidate` is internal), and `Server_kernel_falls_back_to_the_dapr_http_port_when_no_endpoint_is_configured`; the rejection case now uses port 3501 so it is distinguishable from the fallback. The loader's null/blank index-entry guard is **unreachable, not untested**: `TenantReference` and `MagicLinkCapabilityId` reject blank values in their constructors, and STJ deserializes those records through the same constructors, so a degenerate entry cannot exist. Guard left in place as defence in depth; no test added.
- [x] [Review][Patch] `LaunchReadinessTests` pins exact cumulative suite counts and a calendar date — `"ArchitectureTests 54 total / 54 pass"`, `"Projections.Tests 123 / 123 pass"`, `"Server.Tests 443 / 443 pass"`, `"Final total: 876 tests, 872 pass, 4 intentional skips, 0 failures"`, `"2026-09-14"`. Any test added anywhere in the solution reddens the ArchitectureTests lane — including a 55th test in the very project hosting the assertion — and the cheapest way back to green is hand-editing prose, inverting the evidence discipline these tests exist to enforce. Assert the shape (a dated run, zero failures, declared skips) instead of the arithmetic. [tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/LaunchReadinessTests.cs:88-95] **Applied:** the four exact-count assertions and the hard-coded date are replaced by shape assertions (a date-shaped run, the three suite names, and a `Final total: … 0 failures` pattern). The document's inventory is also refreshed to the post-review totals.
- [x] [Review][Patch] Over-broad `catch (Exception)` turns deterministic defects into permanent retry loops, with no diagnostics — both handlers map every non-`IndexCandidateConflictException` failure to `Retryable(DeliveryStateUnavailable)`, and the catch also wraps the `Merge`/`ApplyChecked` delegate running inside `ReadModelWritePolicy.UpdateAsync`, so a fold bug or an NRE on a persisted model with a null `ProjectionFreshness`/`Entries` is reported to the coordinator as a transient store outage and retried forever. Neither handler passes the `context`/`logger` arguments `UpdateAsync` accepts, so nothing on the new write path can ever be logged — while the diff adds an otherwise unused `Microsoft.Extensions.Logging.Abstractions` reference to the project. Narrow the catch to store/transport types and thread a logger through. [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs:78-84, src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs:79-89, src/Hexalith.Timesheets.Projections/Hexalith.Timesheets.Projections.csproj:7-9] **Applied:** both handlers wrap their fold/merge delegate in `Guarded(...)`, which tags deterministic failures as the new internal `ProjectionFoldException` → `Failed(HandlerFailure)`, while store failures and an exhausted retry budget stay `Retryable`. Type-based narrowing alone was wrong and was caught by `Live_writers_retry_optimistic_concurrency_and_report_exhaustion`: `ReadModelWritePolicy` signals retry exhaustion with `InvalidOperationException` too. Both handlers now pass a `ReadModelWriteContext` (correlation-safe fields only) and a logger; the index handler uses an explicit log category rather than `ILogger<T>` because its own type name trips the `DiagnosticsPrivacyTests` source scan.
- [x] [Review][Patch] Dead code left by the paging rewrite — the first `hasMore` assignment is always either overwritten by the second or unreachable past the `break`; `OrderedDistinct` can no longer filter anything now that `ReadAllEventsAsync` normalizes by sequence and rejects duplicate message ids before both call sites. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:340-352,392-400] **Applied in part:** the redundant first `hasMore` assignment is removed. `OrderedDistinct` is retained — it is genuinely unreachable-as-a-filter today, but it is the only de-duplication guard at its two call sites and deleting it would leave that invariant resting solely on `ReadAllEventsAsync`.

**Deferred**

- [x] [Review][Defer] CLAUDE.md's known-pitfall entry is now false and actively misdirects the next agent — it states `MagicLinkTokenHashCapabilityIndexProjection` "has no projection-host wiring; valid links fail closed until a story wires it", which `Program.cs:15-17` now does. Deferred: the fix edits an agent-context file. [CLAUDE.md]
- [x] [Review][Defer] Sprint-status header says "2 decisions resolved and applied, 5 patch action items open, 3 deferred" while the story carries 24 unchecked `[ ]` review items across rounds. Deferred: the fix edits other spec/tracking artifacts. [_bmad-output/implementation-artifacts/sprint-status.yaml:2]
- [x] [Review][Defer] `RuntimeRegistrationTests` mutate `DAPR_HTTP_ENDPOINT`, `DAPR_HTTP_PORT` and `DAPR_API_TOKEN` process-wide while xUnit runs classes in parallel, adding two more writers to a coupling the ledger already records. `DAPR_API_TOKEN` is also read once at registration, so a rotated token never takes effect. Deferred: pre-existing, and the ledger's `IConfiguration` binding item owns the real fix. [tests/Hexalith.Timesheets.Server.Tests/RuntimeRegistrationTests.cs:45-112]

**Rejected**

- `false` — "malformed wire identity escapes both handlers as an unhandled `ArgumentException` through the anonymous `/project/v2` route" (raised by acceptance-auditor and blind-hunter). Refuted at the route: `DomainProjectionDispatcher.ValidateRequest` rejects a null `Request`, blank `TenantId`, blank `AggregateId`, null `Events` and null elements before any handler runs, throwing `ProjectionDispatchValidationException(MalformedOutcome)` which the route returns as `BadRequest` (`DomainProjectionDispatcher.cs:616-627`); handler selection filters on `handler.Domain == request.Domain` (`:96-114`), so a foreign domain selects zero handlers and never reaches `Validate`; and handler invocation is itself wrapped in `catch (Exception)` → `Indeterminate/HandlerFailure` (`:139`). The `Validate()`-outside-`try` placement is unreachable defense-in-depth on this path, not an escape.
- `low` — unbounded paging loop in `ReadAllEventsAsync`. The loop provably advances: each iteration either raises `fromSequence` to `LastSequenceReturned`, or breaks, or throws "did not advance its exclusive sequence cursor"; capability streams hold at most four events and Time Entry streams are bounded. The fix adds a page-budget counter and a new failure mode for a situation not shown to be reachable.
- `low` — `OrderedDistinct`'s blank-`MessageId` branch "silently inverted". The old branch `continue`d on a blank `MessageId`, dropping such events entirely; the new one applies them. That is a correction, not a regression.
- `low` — HTTP cross-tenant coverage collapsed to `"cross-tenant" => null`, byte-identical to `"unknown"`. Real coverage reduction, but the endpoint now derives its tenant from the resolved capability, so an HTTP-level mismatch is undetectable by construction; the loader-level cross-tenant test is the correct and existing home for it.
- rejected per rule — story `Status: in-progress` versus `sprint-status.yaml: review`. The fix edits the spec under review, and status is synced by this workflow's own final step.


**Verification after the review patches** (SDK `10.0.302` per `global.json`; direct xUnit v3 executables, VSTest sockets unavailable):

- `dotnet build Hexalith.Timesheets.slnx -warnaserror` — succeeded, 0 warnings. It did **not** succeed before these patches; see the gitlink decision above.
- ArchitectureTests 54/54, Contracts.Tests 88/88, Projections.Tests 125/125, Server.Tests 444/444, Works.Tests 76/76, IntegrationTests 103 total / 99 pass / 4 intentional skips. Final total: 890 tests, 886 pass, 4 skips, 0 failures.

### Review Findings — loader-core chunk (2026-09-15)

Scope reviewed: the Story 3.6 loader-core chunk (5 files, +1949/-18), including the loader-fold-integrity spec and tests. Four independent layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor.

**Decisions needed**

- [x] [Review][Decision] Project-scoped Activity Types are unreachable through the live magic-link catalog — the loader rejects every `Fresh` catalog item whose scope is not `Tenant`, while issuance and adjustment explicitly accept and validate project-scoped Activity Types and Story 3.4 requires the same Activity Type rules as capture. A correct patch needs a product/architecture choice between project-aware catalog loading and an explicit tenant-only v1 constraint. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:284] **Resolved (tenant-only v1):** magic links may still target Project time entries, but their Activity Type must be tenant-scoped. Project-owned Activity Types require a separately owned project-aware catalog projection; the existing `ProjectForProject(...)` rules are not backed by a production project-addressed writer, so adding that architecture inside this loader review would over-engineer the v1 correction. The implementation must reject project-scoped types consistently during issuance and adjustment, fail closed for such existing capabilities, align the Story 3.4/3.6 contract and tests, and first verify whether any deployed environment contains affected capabilities.
- [x] [Review][Decision] Invalid-link stages remain timing-distinguishable — blank tokens return before any store access, unknown hashes stop after the index, and later failures read the capability, Time Entry, and catalog. The external body stays opaque, but the work profile violates the story's timing-sensitive no-disclosure constraint. The blank-token tests also count only gateway reads, not `IReadModelStore` calls, so their “no read” claim is only partially enforced. Fixing this requires choosing between accepting/documenting the entropy-mitigated risk and designing equal-work or padded handling. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:61] **Resolved (accept and bound the v1 risk):** v1 guarantees indistinguishable external HTTP status, headers, content type, body shape/length, and protected-field absence; it does not promise constant backend work or latency across distributed dependencies. Preserve cheap early exits and exclude dummy reads or response padding: both amplify anonymous traffic while caches, stream sizes, and dependency variance remain distinguishable. The implementation must make the zero-I/O blank-token path explicit by asserting both EventStore and read-model calls, avoid flaky latency tests, and record a launch-readiness risk with a revisit trigger for public exposure without suitable abuse controls, weaker token entropy, or evidence that known invalid tokens can be practically classified. A future requirement for that threat model belongs in a separate security story for a single bounded validation operation.

**Patches**

- [ ] [Review][Patch] Reject a capability whose `ActivityTypeId` disagrees with the loaded Time Entry — the loader compares tenant, entry, contributor, and target but omits the activity type, while description combines the capability's label with the Time Entry's date and duration. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:97]
- [ ] [Review][Patch] Add an opaque-denial regression test for an index read-model exception; the catch exists, but no test would catch a refactor that leaks the failure as a 500. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:96]
- [ ] [Review][Patch] Exercise `TimeEntrySubmitted` through the loader fold; removing that case could make a submitted entry appear `Draft` and wrongly admit adjustment. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:28]
- [ ] [Review][Patch] Falsify the `EventCount` page-integrity guard with a metadata mismatch test. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:632]
- [ ] [Review][Patch] Enforce the resolved tenant-only Activity Type boundary for magic-link v1 — keep Project time-entry targets supported, reject project-scoped Activity Types consistently during issuance and adjustment, fail closed for existing capabilities that cannot resolve to a tenant-scoped type, update Story 3.4/3.6 contract wording and tests, and verify whether any deployed environment contains affected capabilities before treating the rule as migration-free. [src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs:529]
- [ ] [Review][Patch] Align the no-disclosure evidence with the accepted bounded timing guarantee — assert that blank tokens perform neither EventStore nor read-model work, retain response-equivalence assertions without flaky latency checks, and record the residual timing risk plus its public-exposure, abuse-control, entropy, and practical-classification revisit triggers in launch readiness. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:96]

**Deferred**

- [x] [Review][Defer] Unknown future authority-stream events can be ignored after a valid state [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:409] — deferred: every current capability and Time Entry event type is recognized; a current producer or planned event whose semantics can invalidate authorization would settle this maybe-false risk.
- [x] [Review][Defer] The paging loop has no total stream-read budget [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:307] — deferred: EventStore caps each page and no reachable oversized or ever-advancing aggregate was demonstrated; evidence of realistic request amplification would settle this maybe-false risk.
- [x] [Review][Defer] Capability, Time Entry, and catalog are separately read snapshots and may be stale relative to one another [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:84] — deferred: the default topology is not live and the downstream append/concurrency behavior is outside this chunk; demonstrate that dispatch can commit from stale loader state without EventStore optimistic revalidation.
- [x] [Review][Defer] The default AppHost has no EventStore resource, so AC1 is not satisfied in a live topology [src/Hexalith.Timesheets.AppHost/Program.cs:22] — deferred: this real High gap is already an explicit infrastructure-owned waiver in `docs/launch-readiness.md`, and this loader-core chunk must preserve topology.
- [x] [Review][Defer] Non-`Fresh` projection states collapse to one unavailable bundle instead of preserving the AC2 vocabulary [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:109] — deferred: this real Medium gap is explicitly acknowledged in the story and code; preserving explicit state requires revisiting the approved no-disclosure bundle design.
- [x] [Review][Defer] The launch-readiness UI revisit condition conflicts with current repository policy [docs/launch-readiness.md:54] — deferred: this is a pre-existing Story 5.1 documentation issue outside the loader-core chunk; current agent guidance requires future UI through FrontComposer and forbids a Timesheets UI project.

**Rejected**

- `false` — [blind-hunter] a second issuance can reopen a terminal capability: the command aggregate rejects issuance once the capability exists, so no valid producer emits that history.
- `false` — [blind-hunter] a second `TimeEntryRecorded` or impossible lifecycle can reset Time Entry state: the aggregate rejects duplicate recording and invalid transitions before persistence.
- `false` — [blind-hunter] confirmation must require the capability's selected Activity Type to remain active/present: confirmation does not choose or change the historical Activity Type; the approved design deliberately requires only a `Fresh` catalog.
- `false` — [blind-hunter] singleton envelope metadata or protection metadata is accepted without validation: EventStore enforces readable payloads and current metadata v1, while null protection metadata is a documented legacy representation.
- `false` — [blind-hunter] the broad loader catch hides defects without diagnostics: it deliberately preserves no-disclosure, rethrows genuine caller cancellation, and the endpoint emits a correlation-safe `Unknown` diagnostic.
- `low` — [blind-hunter] the revoke/expire test name exercises only revocation: expiry folding is covered separately through the token path, leaving only a cosmetic naming issue.
- `low` — [blind-hunter] the paged fixture's default `LatestSequence` is page-local: the test still exercises the truncation cursor, and a separate regression pins the latest-sequence path.
- `false` — [blind-hunter] sprint-status keys are truncated: 64 characters is the intentional convention and the sprint-plan validator reports the current file valid with no problems.
- `false` — [blind-hunter] the loader-fold sub-spec is `done` while the parent story is `review`: the sub-spec is a distinct completed remediation slice, not the parent story's status.
- `false` — [blind-hunter] the Epic 5 retrospective is `done` while the epic is unfinished: `done` records that the retrospective ran, not that the epic completed.
- `false` — [blind-hunter] cross-tenant HTTP evidence is absent: that integration-test file is intentionally assigned to the next review chunk, not omitted from the Story 3.6 implementation.
- `low` — [blind-hunter] launch-readiness still names an old June baseline: later evidence is explicitly dated, so the older baseline is historical context rather than the asserted current verification.
- `false` — [edge-case-hunter] a non-`ArgumentException` token-generator failure should be converted to denial: the production generator uses argument exceptions for invalid tokens; resource and programming failures should not be silently reclassified.
- `false` — [edge-case-hunter] a second issuance reopens a terminal capability: duplicate of the invalid-history claim; the capability aggregate rejects the producer command.
- `false` — [edge-case-hunter] impossible Time Entry lifecycle histories are silently folded: duplicate of the invalid-history claim; the aggregate prevents their production.
- `false` — [edge-case-hunter] unsupported singleton `MetadataVersion` is accepted: current persisted events use version 1 and EventStore validates readable envelopes before returning them.
- `false` — [edge-case-hunter] sprint-status keys are accidentally truncated: duplicate of the validated 64-character convention claim.
- `false` — [acceptance-auditor] the same `MessageId` at different sequences should be de-duplicated: that is contradictory stream identity, not an exact replay; exact same-sequence duplicate envelopes are already accepted.

### Review Findings — loader-core rerun (2026-09-15)

**Patches**

- [ ] [Review][Patch] Reject a loaded bundle when the capability and Time Entry `ActivityTypeId` values differ; otherwise description can combine one Activity Type's label with another entry's evidence, and confirmation scope still accepts it. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:97]
- [ ] [Review][Patch] Add an opaque fail-closed regression for an exception from the token-hash index read; the production catch exists, but the focused and HTTP read-model doubles cannot currently throw on that branch. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:96]
- [ ] [Review][Patch] Exercise a nontrivial post-recording Time Entry history through the loader: assert `TimeEntrySubmitted` changes approval state, falsify the tenant guard on a later event, and compare the complete observable folded state across ordering/duplicate variants. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:28]
- [ ] [Review][Patch] Falsify the stream-page `EventCount` integrity guard with metadata that disagrees with the actual event collection. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:632]
- [ ] [Review][Patch] Complete the accepted bounded-timing evidence: record index/catalog read-model calls, assert blank and hash-rejected tokens perform zero EventStore and read-model work, and document the residual timing risk plus its approved revisit triggers in launch readiness. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:451]
- [ ] [Review][Patch] Add the required XML documentation to the new public loader type and its public interface implementations. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:16]
- [ ] [Review][Patch] Rename the private static `JsonOptions` field to the repository-required `_camelCase` form. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:24]
- [ ] [Review][Patch] Rename the newly added underscore-delimited test methods to PascalCase, as required by the repository testing convention. [tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs:28]

**Deferred**

- [x] [Review][Defer] Unknown future capability or Time Entry authority events are silently ignored after otherwise valid state. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:409] — deferred: every event emitted by current producers is recognized; a current or planned authorization-changing event would settle whether unknown-event tolerance can make a stale link usable.
- [x] [Review][Defer] Capability, Time Entry, and catalog are read as separate snapshots without a final capability revision check. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:84] — deferred: establish whether downstream EventStore dispatch can commit from this stale loaded state without optimistic revalidation; the default topology does not currently provide that live path.
- [x] [Review][Defer] Non-`Fresh` catalog states collapse to one `Unavailable` bundle instead of preserving the AC2 freshness vocabulary. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:109] — deferred: this real Medium gap is already an explicit design deferral; preserving the individual states requires changing the approved no-disclosure bundle contract.

**Rejected**

- `false` — [blind-hunter] external token success is untested without a matching ambient tenant: the concrete-loader HTTP journey deliberately installs mismatched tenant claims and still proves all four valid routes, so candidate-resolved tenant authority is covered.
- `false` — [blind-hunter] a conflicting ambient tenant could control external-path reads: the same concrete-loader HTTP journey uses `tenant-2` claims while asserting reads and projected candidates remain scoped to `tenant-1`.
- `false` — [blind-hunter] the gateway double permits wrong-domain requests and removal of a required 500-event bound: a wrong requested domain is rejected by the loader's independent response-domain comparison, and no contract requires exactly 500 rather than the EventStore-enforced cap.
- `false` — [blind-hunter] the project-scope fixture fails two predicates and cannot isolate the tenant-only rule: its `Project` is null, so only `Scope != Tenant` rejects it; the fixture isolates that predicate exactly.
- `false` — [edge-case-hunter] a second issuance can reopen a terminal capability: `HandleIssue` rejects issuance whenever capability state already exists, so no valid producer emits that lifecycle.
- `false` — [edge-case-hunter] a second `TimeEntryRecorded` can reset a transitioned entry: the Time Entry aggregate rejects duplicate recording and invalid pre-record transitions before persistence.
- `false` — [edge-case-hunter] provider-opaque bytes can reach this loader and be interpreted as JSON: the EventStore replay endpoint returns an unreadable-protected-data problem before constructing a successful `StreamReadPage`, and the gateway throws on that non-success response.


### Review Findings — loader policy remediation (2026-09-16)

Scope reviewed: `ab4b48f..HEAD` (28 files, +1029/-104, 1892 diff lines), the increment specified by `spec-3-6-implement-eventstore-backed-magic-link-state-loading-2.md`. Four independent layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor. The verification-gap layer filed **no verification gaps**.

**Decisions needed**

- [x] [Review][Decision] Issuance can mint a permanently dead capability — `IssueAsync` receives only the capability state and the Activity Type catalog, never the target `TimeEntryState`, so it validates `command.Scope.ActivityTypeId` against the tenant catalog but never against the entry's own recorded `ActivityTypeId`. This diff's new gates (`EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:114-117` and `ValidateConfirmationScope`/`ValidateAdjustmentScope`) then reject that mismatch on every route, forever and opaquely. The issue endpoint still returns `200` with a real token, which is delivered to the contributor and can never be used. Before this diff the mismatched link worked (with mismatched evidence), so the change converts a display defect into a silent permanent failure. Options: (a) load and check the target Time Entry at issuance and reject there; (b) accept it and document that issuance does not pre-validate entry/capability Activity Type agreement. **Resolved: option B.** Issuance has never loaded the target Time Entry, so `TimeEntryId`, `IsRecorded`, `Contributor`, `Target`, `ActivityTypeId`, and `ActivityTypeScope` are all use-time checks; this increment added the last two and widened a pre-existing gap rather than creating one. Validating only `ActivityTypeId` at issuance would fix one of six and imply a pre-validation that still would not exist, and exposing `LoadTimeEntryAsync` would break the Story 3.4 interface signatures that Story 3.6 is required to preserve. Record the issuance-versus-use validation boundary in the launch-readiness ownership inventory; fail-fast issuance covering all six facts belongs to Story 3.2, where the issue path and its contract live. [src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs:35]
- [x] [Review][Decision] Four `references/` submodule gitlinks move inside the reviewed range — `references/Hexalith.Conversations`, `Hexalith.EventStore`, `Hexalith.FrontComposer`, and `Hexalith.Works` are bumped by commit `1752397`, which is inside `ab4b48f..HEAD`. No task in the spec owns those pointers; the spec's `Never` clause forbids sibling edits and the repository pitfall forbids staging `references/` gitlinks unless the change owns them. Triage rows R2-BH-11/R3-BH-13 dismissed this as predating the run, but only `e2e0755` is below the baseline — `1752397` is not. The build and test evidence were produced with these pins in effect, so nothing is known to be broken; the question is whether the bumps are intended to ship with Story 3.6. Options: (a) keep them and record them as owned by this increment; (b) split them into their own commit/story outside the 3.6 range. **Resolved: option A.** The four sibling pointer moves are intended and ship with Story 3.6; record them as owned by this increment so the spec's `Never: sibling edits` clause and the triage rows that mis-attributed them to a pre-baseline commit no longer contradict the tracked state. [references/Hexalith.EventStore]

**Patches**

- [x] [Review][Patch] Launch-readiness pins test counts that the suite does not produce — the record claims `Projections.Tests 145 / 145` and `Server.Tests 469 / 469` with a `Final total: 939 tests, 935 pass`. The built xUnit v3 executables at this commit report Projections.Tests **146** and Server.Tests **471**, for a real total of **942 tests, 938 pass, 4 skipped**. The stale numbers are repeated in the "Projection rebuild/idempotency" and "Tenant-isolation/security" rows. All tests pass, so this is false release evidence, not a failing gate; nothing pins these counts, so they can drift again. [docs/launch-readiness.md:115]
- [x] [Review][Patch] The loader's catalog ownership arms are unreachable, and readiness presents them as an active control — `LoadActivityTypeCatalogAsync` already collapses the catalog to `UnavailableCatalog()` when any item has `Scope != Tenant`, a non-null `Project`, a blank id/label, or a duplicate id, so once the `Fresh` gate passes, `matchingActivityTypes[0].Scope != Tenant`, `.Project is not null`, and the duplicate arm can never fire; only `Length == 0` does work. `TenantActivityTypeCatalogProjection` likewise admits only `Scope: Tenant, Project: null`, so the project-owned rejection tests must fabricate a catalog the canonical writer cannot emit. Keep the checks as defence in depth but say so in a comment (the adjacent `Fresh` gate has six lines of comment; this block has none), and qualify the readiness evidence so the real gate — the entry-side `ActivityTypeScope` check — is the one claimed. [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:135]
- [x] [Review][Patch] Record that any Activity-Type-changing correction now invalidates outstanding links — the new `timeEntryState.ActivityTypeId == capabilityState.ActivityTypeId` conjunct makes an authorized correction between two *tenant-owned* Activity Types kill every outstanding link for that entry, opaquely and with no operator signal. The readiness ownership inventory frames revoke/reissue only around project-owned and legacy cross-scope repair, so this third, entirely routine history is unlisted. The fail-closed behaviour is correct; the evidence is incomplete. [docs/launch-readiness.md:115]
- [x] [Review][Patch] The removed Work/Project rejection branches changed the issuance rejection code with no test pinning it — a Work target with a project-scoped Activity Type previously returned `AuthorityCannotBeResolved` / `work-project-unresolved`; it now returns `ActivityTypeScopeMismatch` / `scope-mismatch`. The consolidation is correct under the tenant-only policy, but no magic-link test asserts either code or reason, so the outcome contract can move again unnoticed. Assert the code and reason in the existing rejection test. [src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs:610]
- [x] [Review][Patch] The new structured-row fitness assertions are weaker than intended — `ReadClassificationRow` splits with `RemoveEmptyEntries`, so an empty cell shifts every later index; the `cells.Length.ShouldBe(7)` guard does catch it, but reports the misleading "must retain every structured column", and the helper cannot be reused for rows that legitimately use `-` placeholders. The ownership row also leaves cell `[1]` ("Verified state") unasserted while the timing row asserts its own. Split without `RemoveEmptyEntries` and trim the leading/trailing empties explicitly, and assert the ownership row's verified-state cell. [tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/LaunchReadinessTests.cs:288]
- [x] [Review][Patch] The raw-length assertions are triplicated and fully qualified — three sites write `System.Text.Encoding.UTF8.GetByteCount(...)` inline because the file has no `using System.Text;`. Add the using and a `RawByteLength` helper. Note that the compared raw bodies still include `traceId`, which `NormalizeProblemJson` strips elsewhere, so equality holds only while every response's trace identifier has the same length — true for a W3C `Activity` id, not for the `HttpContext.TraceIdentifier` fallback; state that assumption beside the assertion. [tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs:79]
- [x] [Review][Patch] The new OpenAPI `activityTypeScope` is not marked read-only or documented as server-derived — it is published as `anyOf: [$ref ActivityTypeScope, null]` with no `readOnly` and no description, on a `TimeEntryCorrectionValues` schema that `TimesheetsMetadataCatalog` references as `correctedValues` on the `correct-rejected-time-entry` and `correct-approved-time-entry` command surfaces. There is no live authority hole — the type is bound only by events and read models, never by an inbound command record — but the contract advertises a server-derived field as writable to form generators, against the spec's "absent means legacy, never caller authority". Mark it `readOnly` and describe it; update the exact-shape contract assertion with it. [src/Hexalith.Timesheets.Contracts/openapi/timesheets-capture-contracts.v1.json]
- [x] [Review][Patch] `MatchesCorrectionValues` needs a comment for its deliberate legacy-null semantics — a legacy retry whose recorded scope is `null` matches an expected `Tenant` scope and returns NoOp, so the entry's scope is never repaired. That is the approved policy, but nothing says so, and the obvious future "fix" of treating `null` as a mismatch would break legacy idempotency. Same for the `?? current` fallbacks in `TimeEntryState.cs:203,228` and `TimeEntryEvidenceProjection.cs:261,302`. [src/Hexalith.Timesheets.Server/TimeEntries/TimeEntry.cs:1391]
- [x] [Review][Patch] The nested adjustment scope is write-only — `ToAdjustmentValues` stamps `ActivityTypeScope` into `PreviousValues`/`AdjustedValues`, but both folds read the authoritative top-level `adjusted.ActivityTypeScope`. Nothing reads or reconciles the nested value, so the diff adds a divergence surface; triage rejected adding a fold guard (R2-BH-07/R3-BH-07) as disproportionate, which leaves an assertion as the proportionate control. Assert `adjusted.ActivityTypeScope == adjusted.AdjustedValues.ActivityTypeScope` in the writer test, or stop populating the nested field for adjustments. [src/Hexalith.Timesheets.Server/TimeEntries/TimeEntry.cs:1376]
- [x] [Review][Patch] The three new deferred-work rows use absolute machine paths — `source_spec: /home/administrator/projects/hexalith/timesheets/_bmad-output/...` where most of the ledger uses repo-relative paths, and the ledger itself already carries a deferred item about hard-coded machine paths. (Two older rows are absolute too, so this extends an existing inconsistency rather than creating one.) Make the new rows relative. [_bmad-output/implementation-artifacts/deferred-work.md:195]
- [x] [Review][Patch] Serialized replay is proven end-to-end only for `TimeEntryCorrected` — the loader tests genuinely serialize to a stream payload and fold through the production deserializer, but `TimeEntryApprovedCorrected` and `TimeEntryAdjustedThroughMagicLink` scope lineage is exercised only from in-memory objects. The shared nested shape is covered by the contract round-trip test, so residual risk is small, but no test round-trips those two event envelopes carrying the new member. [tests/Hexalith.Timesheets.Server.Tests/TimeEntryAggregateTests.cs]
- [x] [Review][Patch] No reject-on-mismatch test on the rejected-correction retry path — `LegacyRejectedCorrectionRetainsPriorScopeAndSameCrossScopeRetryIsNoOp` covers the NoOp case, and the approved path covers the false branch of the shared `MatchesCorrectionValues`, but nothing proves the rejected path's fall-through to `ValidateCorrection` rejects rather than emitting a second `TimeEntryCorrected`. The helper is shared, so this is symmetry rather than an uncovered branch. [tests/Hexalith.Timesheets.Server.Tests/TimeEntryAggregateTests.cs:1018]
- [x] [Review][Patch] Story tracking artifacts drifted — the Story 3.6 File List still lists only the original loader files and omits everything this remediation touched (`TimeEntryCorrectionValues.cs`, the OpenAPI document, `TimeEntry.cs`, `TimeEntryState.cs`, `TimeEntryEvidenceProjection.cs`, `ApprovedTimeLedgerRowReadModel.cs`), and `sprint-status.yaml` dropped the time component from `last_updated` (`09-15-2026 19:44` → `09-16-2026`). [_bmad-output/implementation-artifacts/3-6-implement-eventstore-backed-magic-link-state-loading.md:476]

**Deferred**

- [x] [Review][Defer] Confirm has no Activity Type availability gate while describe does [src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs:507] — deferred: pre-existing. `DescribeAsync` gates on `TryResolveDisplayLabel`, which requires `IsActive && IsAvailableForCapture`; `ConfirmAsync` receives no catalog at all, so `ValidateConfirmationScope` cannot see availability. An Activity Type deactivated after issuance therefore yields GET 403 / POST 200, and confirmation still emits its event. This diff tightened three gates symmetrically across display/confirm/adjust and left this one asymmetric. Carried as BH-01 / R2-EC-01 / R3-BH-01 / R3-EC-01 — now ledgered for the first time.
- [x] [Review][Defer] The tenant Activity Type catalog does not model per-Project restrictions [src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjection.cs] — deferred: pre-existing. The magic-link path consumes the tenant catalog, while project restriction availability is computed only by `ProjectForProject`, so a restricted tenant-owned Activity Type can still be selected for a Project target. Carried as BH-02 / R2-BH-04 / R3-BH-02 — now ledgered for the first time.
- [x] [Review][Defer] Agent-context files still describe the token-hash index as unwired [CLAUDE.md:105] — deferred: the fix edits agent-context files (`CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`), which is outside a code review's remit. All three still state that `MagicLinkTokenHashCapabilityIndexProjection` "has no projection-host wiring; valid links fail closed until a story wires it", which the shipped handlers and this story's own supersession notice contradict — and which the new fitness test now asserts the readiness document must *not* say. Already ledgered on 2026-09-13 and still open; ageing, and every agent session loads it as fact about a security-relevant path.

**Rejected**

- `false` — [blind-hunter + edge-case-hunter] the capability↔entry identity gate passes when both `ActivityTypeId` values are null: `MagicLinkConfirmationCapabilityIssued.ActivityTypeId` and `TimeEntryRecorded.ActivityTypeId` are both declared non-nullable, and `LoadTokenStateAsync` rejects `capability.ActivityTypeId is null` before either validator runs. Neither layer showed a producer that reaches the state.
- `false` — [edge-case-hunter] an explicit `ActivityTypeScope.Unknown` in a correction payload overwrites a valid Tenant scope: `TimeEntryRecorded.ActivityTypeScope` is a non-nullable positional parameter and `ValidateRecordedFacts` rejects `Unknown` at recording, so state scope is never `Unknown`; every sanctioned `CorrectedValues` scope is server-resolved and validated `!= Unknown` before emission. Reaching it needs malformed external history.
- `false` — [edge-case-hunter] a superseded ledger row is labelled with the post-correction scope when `PreviousValues` omits scope but `CorrectedValues` carries a change: `ToCorrectionValues(TimeEntryState)` sets `ActivityTypeScope` unconditionally, so new writers always emit both snapshots and legacy writers omit both. A one-sided payload requires malformed external history. Duplicate of R2-EC-03 / R3-EC-02, rejected on the same grounds.
- `false` — [blind-hunter + acceptance-auditor] `_jsonOptions` breaks the repository naming convention: `.editorconfig` requires the `_` prefix and camelCase for **all** private fields (`applicable_kinds = field`, `applicable_accessibilities = private`, with no modifier restriction), so this file is the only conformant site and the five `s_jsonOptions` fields are the deviation. The acceptance-auditor's premise that the rule covers only private *instance* fields is incorrect. The rename was also explicitly requested by the 2026-09-15 review round.
- `low` — [blind-hunter + edge-case-hunter] the spec's triage rows R3-BH-01/R3-BH-02 justify skipping a ledger entry by citing a prior deferred-work entry that does not exist: the observation is correct — neither risk was ever ledgered — but the proposed fix edits the Review Triage Log of the spec under review, which review policy rejects. The substance is resolved instead by the two new `defer` entries above, which ledger both risks for the first time.


## Dev Notes

### Source Documents Loaded

- `_bmad-output/planning-artifacts/epics.md` — Epic 3 and Story 3.6 (requirements FR14, FR21, FR22, NFR1, NFR6, NFR8, NFR9, NFR12).
- `_bmad-output/planning-artifacts/architecture.md` — Data Architecture, Authentication & Security (Magic-Link Confirmation model + the explicit 2026-06-19/2026-06-20 status notes assigning the concrete loader to **this story**), API & Communication Patterns, Implementation Patterns, Project Structure & Boundaries.
- `_bmad-output/planning-artifacts/prds/prd-timesheets-2026-06-18/prd.md` — FR-14, UJ-3, NFR6/NFR8/NFR12, and the v1 decision that secondary identity verification is deferred post-v1.
- Previous story intelligence from `_bmad-output/implementation-artifacts/3-5-reject-invalid-confirmation-links-without-resource-disclosure.md` (Story 3.6 is its tracked High follow-up).
- Persistent project-context facts from sibling Hexalith modules (EventStore, Tenants, Parties, Projects, Conversations, FrontComposer). No Timesheets-root `project-context.md` exists.
- Read current magic-link server/endpoint/projection code, the EventStore SDK seams, and DI registration listed under References.

### Epic And Story Context

- Epic 3 lets external contributors confirm/adjust scoped time via no-disclosure Magic-Link Confirmation without becoming internal users. Stories 3.1–3.4 built the contracts, capability issuance, confirm/adjust service behavior, projections, endpoints, metadata, and OpenAPI. Story 3.5 hardened invalid-link no-disclosure at the **service** boundary.
- **This story is the explicit, planned home for the one deferred gap.** Story 3.5 left a tracked **High** follow-up: *"Implement a real EventStore-backed `IMagicLinkConfirmationCapabilityStateLoader` (token-hash index + rebuildable projection) to replace the fail-closed `UnavailableMagicLinkConfirmationCapabilityStateLoader`."* Architecture §Authentication & Security (readiness repair, 2026-06-20) ratifies this: *"the concrete `IMagicLinkConfirmationCapabilityStateLoader` is owned by Epic 3 follow-up Story 3.6 … Epic 5 verifies the evidence only after the owning feature work is complete."*
- Launch-readiness dependency (epics §3.3/§3.4): live host confirm/adjust requires Story 3.6. Until it ships, external live endpoints stay fail-closed by default. This story makes the live host functional **without weakening** the no-disclosure guarantees Story 3.5 proved.
- Out of scope (deferred post-v1, do not add): secondary identity verification (OTP/identity proofing/challenge), a full external portal, internal shell navigation, a token-inspection/browse/debug endpoint, or any new external UI surface. No UI package is created by this story.

### The Core Design Challenge — read this first

The live host fails closed today because the registered loader is a stub. Trace the gap:

- All four external routes (`GET /api/timesheets/magic-links/confirm`, `POST …/confirm/submit`, `GET …/adjust`, `POST …/adjust/submit`) call `stateLoader.LoadTokenStateAsync(t, …)`. The stub returns `new MagicLinkEndpointTokenState(null, null, UnavailableCatalog())`, so `DescribeAsync`/`ConfirmAsync`/`AdjustAsync` always produce `null`/non-dispatch → `Denied()` (403). The internal admin `revoke`/`expire` routes call `LoadCapabilityAsync(command.CapabilityId, …)`, which the stub answers with `null` → those reject too.
- **The unsolved piece is token → capability resolution.** The opaque token is mapped to a capability **only** through `MagicLinkTokenHash`, and that hash is persisted **only inside `MagicLinkConfirmationCapabilityIssued.TokenHash`**. The public `MagicLinkConfirmationCapabilityReadModel` deliberately does **not** expose `TokenHash` (no-disclosure), so it cannot serve as the lookup. You must add a dedicated **rebuildable, EventStore-backed** index `MagicLinkTokenHash → (TenantReference, MagicLinkCapabilityId)` built from `Issued` events.
- **Authority split (do not blur this):** the index/projection is a *candidate resolver* only and is non-authoritative. The single-use / revoked / expired / used decision must come from folding the **authoritative capability aggregate events** and from the existing `MagicLinkConfirmationCapability` validators. A projection must never be the source of single-use or revocation truth.
- **Tenant resolution wrinkle:** the loader interface is fixed (set in Story 3.4) and is tenant-less on `LoadActivityTypeCatalogAsync()` and `LoadCapabilityAsync(capabilityId)`. External contributors send no tenant claim, so for the token path the tenant must be derived from the **resolved capability** (the index returns it), and the catalog/Time Entry/capability streams are read for that tenant. For the admin issue/revoke/expire paths the tenant comes from the caller's trusted context the host already builds (`TimesheetsServerRequestContext.FromTrustedSources(...)`). Decide and document how the concrete loader obtains tenant for each method (resolved-capability vs. ambient request/tenant accessor) — see Open Questions. Never trust a caller-supplied tenant field.

### Current Code State To Extend (exact signatures)

**Seam — `src/Hexalith.Timesheets.Server/MagicLinks/IMagicLinkConfirmationCapabilityStateLoader.cs`:**
```csharp
public interface IMagicLinkConfirmationCapabilityStateLoader
{
    ValueTask<ActivityTypeCatalogReadModel> LoadActivityTypeCatalogAsync(CancellationToken cancellationToken);
    ValueTask<MagicLinkCapabilityState?> LoadCapabilityAsync(MagicLinkCapabilityId capabilityId, CancellationToken cancellationToken);
    ValueTask<MagicLinkEndpointTokenState> LoadTokenStateAsync(string oneTimeToken, CancellationToken cancellationToken);
}
```

**Bundle — `MagicLinkEndpointTokenState.cs`:**
```csharp
public sealed record MagicLinkEndpointTokenState(
    MagicLinkCapabilityState? CapabilityState,
    TimeEntryState? TimeEntryState,
    ActivityTypeCatalogReadModel ActivityTypeCatalog);
```

**Current fail-closed default to replace — `UnavailableMagicLinkConfirmationCapabilityStateLoader.cs`:** returns `null` capability, `(null, null, UnavailableCatalog())` token state, and `new ActivityTypeCatalogReadModel([], ProjectionFreshnessMetadata.Unavailable())`.

**Authoritative capability state — `MagicLinkCapabilityState.cs`** (folded via `Apply`): fields include `Exists`, `CapabilityId`, `Tenant`, `Contributor`, `Target`, `ActivityTypeId`, `TimeEntryId`, `TargetKind`, `AllowedAction`, `State` (enum `Issued`/`Revoked`/`Expired`/`Used`), `ExpiresAtUtc`, `Issuer`, `IssuedAtUtc`, `TokenHash`, `UsedAtUtc`, `UseMetadata`, and `IsTerminal`. Apply overloads exist for `MagicLinkConfirmationCapabilityIssued/Revoked/Expired/Used`. `TokenHash` is set only on `Issued`.

**Validators (no event emission) — `MagicLinkConfirmationCapability.cs`:** public entry points are `IsValidForUse(state, tenant, tokenHash, atUtc)`, `IsValidForAdjustment(...)`, and `HandleUse(...)`/`HandleRevoke`/`HandleExpire`/`HandleIssue` returning `TimesheetsDomainResult`. These delegate to the private `ValidateUse` logic, which checks tenant match, existence, capability id, contributor, Time Entry id, target, `TargetKind == ProposedTimeEntry`, token-hash match, non-terminal/issued state, expiry, and allowed action — all mismatches return fail-closed without disclosing the reason. Call the public validators; do not look for a public `ValidateUse`.

**Command service (loader is NOT called here — the endpoint calls it and passes results in) — `MagicLinkConfirmationCapabilityCommandService.cs`:** `DescribeAsync`/`DescribeAdjustmentAsync` return `MagicLink…DisplayResponse?` (`null` ⇒ deny); `ConfirmAsync`/`AdjustAsync` return a result with `WasDispatched`; all take `capabilityState`, `timeEntryState`, and (for describe/adjust) `activityTypeCatalog`. The service derives the hash internally via `IMagicLinkTokenGenerator.DeriveHash(oneTimeToken)` and re-validates; the loader must supply correctly-folded state, not pre-judge validity.

**Token hashing — `CryptographicMagicLinkTokenGenerator.cs`:** `DeriveHash(string)` = `Base64Url(SHA256(UTF8(token)))` → `MagicLinkTokenHash`. Reuse this exact generator for index lookup so hashes match the persisted `Issued.TokenHash`.

**Endpoints — `src/Hexalith.Timesheets/Endpoints/MagicLinks/MagicLinkConfirmationCapabilityEndpoints.cs`:** wires `IMagicLinkConfirmationCapabilityStateLoader` into all seven routes (3 admin: issue/revoke/expire + 4 external: confirm GET, confirm/submit POST, adjust GET, adjust/submit POST); all denials funnel through one private `Denied()` → `Results.Problem(title: MagicLinkInvalidLinkDenial.Default.Title, detail: …Default.Detail, statusCode: 403)`. Do not change the denial shape.

**DI — `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs:42`:** `services.TryAddSingleton<IMagicLinkConfirmationCapabilityStateLoader, UnavailableMagicLinkConfirmationCapabilityStateLoader>();`. Note: **every read-side reader in this kernel is currently an `Unavailable` fail-closed stub** (`ITimeEntryEvidenceProjectionReader`, `IApprovedTimeLedgerProjectionReader`, etc.) — no concrete EventStore read implementation exists in Timesheets yet, so this loader establishes the concrete EventStore read pattern for the magic-link capability. Keep the fail-closed posture as the default everywhere else.

### Canonical EventStore Fold Pattern (reuse, do not reinvent)

- State objects fold events with `void Apply(SpecificEvent @event)` methods (see `MagicLinkCapabilityState`, `TimeEntryState`, `ActivityTypeCatalogState`). Reconstruct state by ordering events by sequence and applying each once.
- Idempotency/replay: de-duplicate by message id and tolerate duplicates/out-of-order rebuilds — mirror `MagicLinkConfirmationCapabilityProjection.Project(...)` and `TenantActivityTypeCatalogProjection.Project(...)`, which both skip blank/duplicate `MessageId`s and order by `SequenceNumber`.
- EventStore access lives in the `Hexalith.EventStore` SDK: `AggregateIdentity(tenantId, domain, aggregateId)` derives stream/metadata/snapshot keys; the domain-service exposes replay/projection paths; `IReadModelStore` (EventStore.Client) persists read models with ETag concurrency. Read through these seams — Timesheets is domain-centric and must not re-implement actor/Dapr state plumbing or invent key formats.
- Catalog: load via the tenant Activity Type catalog read model and return real `ProjectionFreshnessMetadata` (`Fresh`/`Rebuilding`/`Stale`/`Unavailable`/`Degraded`/`Unknown`). Confirmation/adjustment are trust-bearing and must require `Fresh`; non-fresh fails closed.

### Architecture Constraints

- Domain state persists **only** through Hexalith.EventStore. No SQL/Redis/Dapr-state writes, local files, broker-backed CRUD, static dictionaries, mutable caches, direct projection mutation, or Data Protection-only tokens as magic-link authority. [Source: `_bmad-output/planning-artifacts/architecture.md#Data-Architecture`]
- Magic links are scoped capabilities, not sessions: store only token hash + safe metadata; token values/decoded material are never logged, projected, exported, stored in comments, or accepted in command bodies. [Source: `architecture.md#Magic-Link-Confirmation-model`]
- Tenant/resource gates run **before** aggregate load, command dispatch, projection read, export, or magic-link disclosure; denied/unknown/stale/unavailable outcomes fail closed and avoid existence disclosure. [Source: `architecture.md#Authorization-model`, `#Security-posture`]
- Projections are rebuildable, idempotent, duplicate-tolerant, non-authoritative for writes, and expose freshness/degraded/rebuilding/unavailable states. [Source: `architecture.md#Projection-model`, `#State-Management-Patterns`]
- Magic-link routes are capability-specific; never expose a general token inspection endpoint or browse/query API. [Source: `architecture.md#API-Naming-Conventions`]
- Contracts stay infrastructure-free; Server owns capability logic and validation orchestration; query/read surfaces hide EventStore envelope mechanics. [Source: `architecture.md#Component-Boundaries`, `#Query-API-model`]
- Logs/traces exclude comments, tokens, secrets, command bodies, event payloads, personal data, target names, protected identifiers, and EventStore envelopes. [Source: `architecture.md#Data-Exchange-Formats`]

### Previous Story Intelligence

- Story 3.5 explicitly authorized deferring this loader and recorded it as the **only** open High item; it proved service-level no-disclosure equivalence via the single `Denied()` helper and `MagicLinkInvalidLinkDenial.Default`. This story must preserve that equivalence while adding live-host fold behavior. [Source: `3-5-…disclosure.md#Senior-Developer-Review-AI`]
- Story 3.5 also left **Medium** follow-ups this story can opportunistically close: HTTP-boundary integration tests (`WebApplicationFactory`/`Mvc.Testing`) asserting equal 403/`application/problem+json`/identical ProblemDetails across invalid categories, and wiring `MagicLinkInvalidLinkOutcomeCategory` into privacy-safe diagnostics. Closing the HTTP-boundary test is the natural overlap with Story 3.7; coordinate scope so work is not duplicated.
- `MagicLinkInvalidLinkOutcomeCategory` (Server-internal enum) already enumerates `Malformed`, `UnknownCapability`, `HashMismatch`, `Expired`, `Revoked`, `Used`, `TenantMismatch`, `WrongRecipient`, `WrongAction`, `WrongScope`, `StaleCatalog`, `Unauthorized`, `RateLimited`, `RepeatedAttempt`. Reuse it for internal categorization; keep it out of external responses and out of the Contracts surface.
- Prior magic-link stories kept sibling submodule pointers unchanged. Continue to not modify `Hexalith.EventStore`, `Hexalith.FrontComposer`, `Hexalith.Tenants`, `Hexalith.Parties`, or other submodule files.

### Git Intelligence Summary

- `6349709 feat(story-3.4)` introduced `IMagicLinkConfirmationCapabilityStateLoader`, `MagicLinkEndpointTokenState`, and the `Unavailable` default — i.e. the seam this story fills.
- Story 3.5 (committed in the 3.x line) added `MagicLinkInvalidLinkDenial`, `MagicLinkInvalidLinkOutcomeCategory`, endpoint/service normalization, and disclosure-equivalence tests.
- `3731140 feat(story-1.10)` is a fresh example of a concrete adapter (`WorksQueryWorkReferenceValidator` behind `IWorksQueryChannel`) replacing a `DenyAll`/`Unavailable` default with fail-closed behavior — mirror that shape (concrete impl + DI swap + fail-closed on every unresolved/cross-tenant/ambiguous path) for the loader.
- Local SDK pinned via `global.json` (`10.0.301`, `rollForward: latestPatch`); packages centralized in `Directory.Packages.props`. Baseline HEAD at story creation: `24a37c1`.

### Library And Framework Requirements

- Do not upgrade dependencies or add inline `PackageReference` versions; versions are centralized in `Directory.Packages.props` (.NET 10, xUnit v3 `3.2.2`, Shouldly `4.3.0`, NSubstitute `6.0.0-rc.1`, Dapr SDK `1.18.4` line, Aspire `13.4.5`).
- Tests use xUnit v3 + Shouldly + NSubstitute; run test projects individually, not solution-level `dotnet test`.
- Use the existing `IMagicLinkTokenGenerator`/`CryptographicMagicLinkTokenGenerator` for hashing; do not introduce a second hashing scheme.

### Project Structure Notes

- New/changed loader + index code: `src/Hexalith.Timesheets.Server/MagicLinks/` (concrete loader, token-hash index resolver) and `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs` (DI swap). If a rebuildable index projection is added, place projection/read-model code under `src/Hexalith.Timesheets.Projections/MagicLinks/` and any new read-model contract under `src/Hexalith.Timesheets.Contracts/Models/MagicLinks/` (the index read model must not expose the raw token; storing the hash internally is acceptable).
- Endpoints under `src/Hexalith.Timesheets/Endpoints/MagicLinks/` should need no behavioral change — they already consume the seam; verify, do not rewrite the denial path.
- Tests: `tests/Hexalith.Timesheets.Server.Tests`, `tests/Hexalith.Timesheets.Projections.Tests`, `tests/Hexalith.Timesheets.IntegrationTests`, `tests/Hexalith.Timesheets.Contracts.Tests`, and `tests/Hexalith.Timesheets.ArchitectureTests` (`DiagnosticsPrivacyTests`, dependency-direction). Do not use `_bmad-output/` or `docs/` as implementation scratch space.
- No UI package is introduced by this story (backend loader only); do not add `Hexalith.Timesheets.UI`.

### Testing Standards

- Every claimed fail-closed path needs executable proof; compare external disclosure **structurally** (status code, ProblemDetails title/body, content type, header set, absence of sensitive fields, no reason text, no dispatch side-effects), not only semantically.
- Add fold-determinism tests: same event set in different orders / with duplicates / on rebuild ⇒ identical folded `MagicLinkCapabilityState`, `TimeEntryState`, and catalog trust state.
- Add negative tests for the full invalid set in AC3 plus missing-Time-Entry and stale/unavailable-catalog, asserting they are externally indistinguishable.
- Integration tests must assert end-state (folded state / dispatch outcome), not only return codes or mock call counts. Prefer an in-process HTTP host for the equivalence proof if scope allows.
- Extend `DiagnosticsPrivacyTests` to cover the new index/loader/diagnostics surface.

### Anti-Patterns To Prevent

- Do not make any projection, read model, mutable cache, static dictionary, SQL/Redis/Dapr-state row, local file, or Data Protection token the authority for single-use, revocation, or expiry — fold the authoritative capability aggregate.
- Do not persist or log raw tokens, decoded capability material, command bodies, event payloads, comments, personal data, Party/Project/Work display names, raw claims, upstream errors, or EventStore envelopes.
- Do not distinguish failure categories in any externally observable channel (status, headers, content type, body, length, timing).
- Do not trust caller-supplied tenant/contributor/capability/token-hash fields; derive tenant from trusted context (admin) or the resolved capability (external token).
- Do not add a token-inspection/browse/debug endpoint, a second hashing scheme, secondary identity verification, an external portal, internal shell navigation, or a new UI surface.
- Do not invent EventStore stream/key formats, call Dapr/actor state directly, add inline package versions, create `.sln` files, weaken `-warnaserror`, or modify sibling submodule files.

### Open Questions (resolve during implementation; do not block)

1. **Tenant source per method.** Given the tenant-less `LoadActivityTypeCatalogAsync()`/`LoadCapabilityAsync(capabilityId)` signatures, will the concrete loader obtain tenant from an ambient request/tenant accessor (admin paths) plus the resolved-capability tenant (external token path), or is a small internal helper preferred? Pick the option that keeps tenant strictly trusted and document it in Completion Notes. (Interface signatures are fixed by Story 3.4 and must not change.)
2. **Index mechanism.** Confirm whether the rebuildable token-hash → capability index is best expressed as an `IDomainProjectionHandler`-managed read model via `IReadModelStore`, or a Timesheets projection consumed through the existing projection-reader idiom. Choose the one consistent with how the EventStore SDK exposes reads to a domain-centric module in the live host, and verify the chosen read seam actually exists before coding (all current readers are `Unavailable` stubs, so this is the first concrete read).

### References

- [Source: `_bmad-output/planning-artifacts/epics.md#Story-3-6-Implement-EventStore-Backed-Magic-Link-State-Loading`]
- [Source: `_bmad-output/planning-artifacts/epics.md#Epic-3-External-Contributor-Confirmation`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Authentication-&-Security`] (Magic-Link Confirmation model + 2026-06-19 status note + 2026-06-20 readiness repair assigning the loader to Story 3.6)
- [Source: `_bmad-output/planning-artifacts/architecture.md#Data-Architecture`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Validation-Patterns`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Component-Boundaries`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Data-Exchange-Formats`]
- [Source: `_bmad-output/planning-artifacts/prds/prd-timesheets-2026-06-18/prd.md#FR-14-Support-Magic-Link-Confirmation`]
- [Source: `_bmad-output/implementation-artifacts/3-5-reject-invalid-confirmation-links-without-resource-disclosure.md#Review-Follow-ups-AI`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/IMagicLinkConfirmationCapabilityStateLoader.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/UnavailableMagicLinkConfirmationCapabilityStateLoader.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkEndpointTokenState.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkCapabilityState.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapability.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/CryptographicMagicLinkTokenGenerator.cs`]
- [Source: `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkInvalidLinkOutcomeCategory.cs`]
- [Source: `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs`] (loader registration, line 42)
- [Source: `src/Hexalith.Timesheets.Server/Runtime/TimesheetsEventStoreIntegration.cs`] (`DomainName = "timesheets"`)
- [Source: `src/Hexalith.Timesheets/Endpoints/MagicLinks/MagicLinkConfirmationCapabilityEndpoints.cs`]
- [Source: `src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkConfirmationCapabilityProjection.cs`]
- [Source: `src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjection.cs`]
- [Source: `src/Hexalith.Timesheets.Server/TimeEntries/TimeEntryState.cs`]
- [Source: `src/Hexalith.Timesheets.Server/ActivityTypes/ActivityTypeCatalogState.cs`]
- [Source: `src/Hexalith.Timesheets.Contracts/Models/MagicLinks/MagicLinkConfirmationCapabilityReadModel.cs`] (intentionally omits `TokenHash`)
- [Source: `src/Hexalith.Timesheets.Contracts/Models/ActivityTypeCatalogReadModel.cs`, `ProjectionFreshnessMetadata.cs`]
- [Source: `src/Hexalith.Timesheets.Contracts/Events/MagicLinks/`] (`MagicLinkConfirmationCapabilityIssued/Used/Revoked/Expired`)
- [Source: `Hexalith.EventStore/src/Hexalith.EventStore.Contracts/Identity/AggregateIdentity.cs`]
- [Source: `Hexalith.EventStore/src/Hexalith.EventStore.Client/Projections/IReadModelStore.cs`]
- [Source: `Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainProjectionHandler.cs`, `IDomainQueryHandler.cs`]
- [Source: `README.md#Build-and-Test`] (xUnit v3 direct-executable fallback)
- [Source: `Directory.Packages.props`, `global.json`]

## Story Context Completion Status

Ultimate context engine analysis completed - comprehensive developer guide created.

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Claude Opus 4.8, 1M context) — BMAD dev-story workflow.

### Debug Log References

- `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet restore Hexalith.Timesheets.slnx -m:1 /nr:false` → exit 0.
- `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet build Hexalith.Timesheets.slnx --no-restore -warnaserror -m:1 /nr:false` → exit 0, **0 Warning(s), 0 Error(s)**. (A first incremental run surfaced 18 IDE0065/NU5118/NU5128 errors confined to the unrelated `Hexalith.PolymorphicSerializations` submodule packaging step; no Timesheets project ever errored, and a clean rebuild reports 0/0.)
- Affected test lanes were run via the README direct xUnit v3 executable fallback (`tests/<Project>/bin/Debug/net10.0/<Project>`) because `dotnet test` is blocked by local VSTest socket permissions (`SocketException (13): Permission denied`). Final results:
  - ArchitectureTests: 27 passed / 0 failed.
  - Contracts.Tests: 86 passed / 0 failed.
  - Server.Tests: 395 passed / 0 failed (was 384; +11 new loader tests).
  - Projections.Tests: 77 passed / 0 failed.
  - IntegrationTests: 66 passed / 0 failed / 3 skipped (pre-existing infrastructure/performance lanes awaiting a runtime data-bearing story — not touched by this story).

### Completion Notes List

Implemented the concrete EventStore-backed `IMagicLinkConfirmationCapabilityStateLoader` (`EventStoreMagicLinkConfirmationCapabilityStateLoader`) and replaced the fail-closed `Unavailable` default in DI — closing Story 3.5's only open High follow-up and making the live host functional for the magic-link confirm/adjust/describe and admin revoke/expire paths without weakening no-disclosure.

**Design decisions / Open Questions resolved:**

1. **Tenant source per method (Open Question #1).** Introduced `ITimesheetsTrustedContextAccessor` (Server seam) with a fail-closed `UnavailableTimesheetsTrustedContextAccessor` default and an `HttpContextTimesheetsTrustedContextAccessor` host adapter (claims-based, registered via `Replace` in `Program.cs`). The admin methods (`LoadCapabilityAsync`, `LoadActivityTypeCatalogAsync`) derive tenant from this trusted ambient context — never a caller-supplied field — and fail closed (null / Unavailable catalog) when no trusted tenant is present. The external token path derives tenant from the **resolved capability** returned by the index. Interface signatures were left exactly as set in Story 3.4.
2. **Index mechanism (Open Question #2).** The token-hash → `(TenantReference, MagicLinkCapabilityId)` index is a rebuildable Timesheets projection (`MagicLinkTokenHashCapabilityIndexProjection`, static `Rebuild`/`Apply` fold from `MagicLinkConfirmationCapabilityIssued` events only) persisted/read through the platform `IReadModelStore` seam — the same pure-fold convention every existing Timesheets projection uses (`MagicLinkConfirmationCapabilityProjection`, `TenantActivityTypeCatalogProjection`). The read model stores only the token **hash** (never the raw token / decoded material) and is a non-authoritative candidate resolver.

**Authority split.** The index resolves only a candidate. Single-use / revoked / expired / used / wrong-tenant / wrong-hash truth comes solely from folding the authoritative capability aggregate (`MagicLinkCapabilityState.Apply`) and the public `MagicLinkConfirmationCapability.IsValidForUse/IsValidForAdjustment/HandleUse` validators in the command service. The loader supplies correctly-folded state and never pre-judges validity. Proven by `LoadTokenStateAsync_returns_folded_revoked_state_proving_index_is_not_revocation_authority` and `..._uses_folded_capability_state_as_single_use_authority`.

**Fold pattern.** Reads flow only through the EventStore SDK seams `IEventStoreGatewayClient.ReadStreamAsync` (paged, `DomainName = "timesheets"`, `(tenant, domain, aggregateId)` shape, `aggregateId == null` for the domain-wide catalog rebuild read) and `IReadModelStore` — no direct Dapr/actor/state access and no invented key formats. Events are ordered by `SequenceNumber` and de-duplicated by `MessageId` (idempotent, replay-safe, duplicate-tolerant), mirroring the existing projections. Determinism proven by `LoadTokenStateAsync_folds_capability_deterministically_across_orderings_and_duplicates`.

**No-disclosure.** Every loader resolution stage that can fail — blank/malformed token, hash-not-in-index, missing capability aggregate, folded-hash mismatch, cross-tenant mismatch, unrecorded Time Entry, and EventStore read failure — collapses to the identical opaque `MagicLinkEndpointTokenState(null, null, UnavailableCatalog())` (asserted via the shared `ShouldBeOpaqueFailClosed` helper across 5 new negative tests). Catalog read failure surfaces an explicit `Unavailable` freshness so the trust-bearing command service fails closed downstream rather than returning a catalog it could not load. The loader logs nothing and persists nothing (read-only candidate resolver), guarded by a new `DiagnosticsPrivacyTests` fitness test. Service-/endpoint-level disclosure equivalence across the full AC3 invalid set (incl. stale catalog) remains proven by the existing `MagicLinkConfirmationCapabilityCommandServiceTests`.

**Freshness.** `AdjustAsync` and the describe paths require `ProjectionFreshnessState.Fresh` and fail closed otherwise (covered by the existing command-service Stale-catalog cases). `ConfirmAsync` does not consume the catalog (it confirms the proposed entry as-is), so the freshness gate applies to the catalog-consuming adjust/describe paths.

**Scope note / deferral.** The full HTTP-boundary `WebApplicationFactory`/`Mvc.Testing` equivalence test (Story 3.5 Medium follow-up #2, overlapping Story 3.7) is **not** added here: the Timesheets module has no runtime host fixtures yet (all read-side readers are `Unavailable` stubs and the IntegrationTests infrastructure/EventStore/Dapr lane is deliberately skipped pending a data-bearing story). Coverage is kept at loader + service level as the story permits; the HTTP-boundary proof remains the natural home of Story 3.7. The live projection-host wiring that populates the index read model from `Issued` events in the running topology likewise belongs to that runtime story — consistent with every other Timesheets projection, which today ships its pure fold + read seam only.

### File List

Current cumulative Story 3.6 inventory, including its accepted review increments:

- Runtime and hosting: `README.md`; `aspire.config.json`; `src/Hexalith.Timesheets.AppHost/Hexalith.Timesheets.AppHost.csproj`; `src/Hexalith.Timesheets.AppHost/Program.cs`; `src/Hexalith.Timesheets/Hexalith.Timesheets.csproj`; `src/Hexalith.Timesheets/Program.cs`; `src/Hexalith.Timesheets/Endpoints/MagicLinks/MagicLinkConfirmationCapabilityEndpoints.cs`; `src/Hexalith.Timesheets/Runtime/HttpContextTimesheetsTrustedContextAccessor.cs`; `src/Hexalith.Timesheets/Runtime/InternalSurfaceGuard.cs`; `src/Hexalith.Timesheets/Runtime/InternalSurfaceOptions.cs`.
- Contracts: `src/Hexalith.Timesheets.Contracts/Models/ApprovedTimeLedgerRowReadModel.cs`; `src/Hexalith.Timesheets.Contracts/Models/TimeEntryCorrectionValues.cs`; `src/Hexalith.Timesheets.Contracts/openapi/timesheets-capture-contracts.v1.json`.
- Server: `src/Hexalith.Timesheets.Server/Hexalith.Timesheets.Server.csproj`; `src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkActivityTypeCatalogReadModelAddress.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkConfirmationCapabilityCommandService.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkExternalRequestContext.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkTokenHashCapabilityIndexEntry.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkTokenHashCapabilityIndexProjection.cs`; `src/Hexalith.Timesheets.Server/MagicLinks/MagicLinkTokenHashCapabilityIndexReadModel.cs`; `src/Hexalith.Timesheets.Server/Runtime/ITimesheetsTrustedContextAccessor.cs`; `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs`; `src/Hexalith.Timesheets.Server/Runtime/UnavailableTimesheetsTrustedContextAccessor.cs`; `src/Hexalith.Timesheets.Server/TimeEntries/TimeEntry.cs`; `src/Hexalith.Timesheets.Server/TimeEntries/TimeEntryState.cs`.
- Projections: `src/Hexalith.Timesheets.Projections/Hexalith.Timesheets.Projections.csproj`; `src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs`; `src/Hexalith.Timesheets.Projections/MagicLinks/IndexCandidateConflictException.cs`; `src/Hexalith.Timesheets.Projections/MagicLinks/IndexRebuildCandidate.cs`; `src/Hexalith.Timesheets.Projections/MagicLinks/IndexRebuildCandidateEntry.cs`; `src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs`; `src/Hexalith.Timesheets.Projections/ProjectionEventReader.cs`; `src/Hexalith.Timesheets.Projections/ProjectionFoldException.cs`; `src/Hexalith.Timesheets.Projections/ReadModelRebuildConcurrency.cs`; `src/Hexalith.Timesheets.Projections/TimeEntries/TimeEntryEvidenceProjection.cs`.
- Tests: `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/DependencyDirectionTests.cs`; `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/DiagnosticsPrivacyTests.cs`; `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/LaunchReadinessTests.cs`; `tests/Hexalith.Timesheets.Contracts.Tests/TimeCaptureContractTests.cs`; `tests/Hexalith.Timesheets.IntegrationTests/Hexalith.Timesheets.IntegrationTests.csproj`; `tests/Hexalith.Timesheets.IntegrationTests/InternalSurfaceGuardTests.cs`; `tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs`; `tests/Hexalith.Timesheets.Projections.Tests/Hexalith.Timesheets.Projections.Tests.csproj`; `tests/Hexalith.Timesheets.Projections.Tests/MagicLinkStateProjectionHandlerTests.cs`; `tests/Hexalith.Timesheets.Projections.Tests/TimeEntryEvidenceProjectionTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/MagicLinkConfirmationCapabilityCommandServiceTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/RuntimeRegistrationTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/TimeEntryAggregateTests.cs`.
- Evidence and tracking: `docs/launch-readiness.md`; `_bmad-output/implementation-artifacts/3-2-issue-scoped-magic-link-confirmation-capabilities.md`; `_bmad-output/implementation-artifacts/3-3-confirm-time-through-magic-link.md`; `_bmad-output/implementation-artifacts/3-4-adjust-time-through-magic-link.md`; `_bmad-output/implementation-artifacts/3-5-reject-invalid-confirmation-links-without-resource-disclosure.md`; `_bmad-output/implementation-artifacts/3-6-implement-eventstore-backed-magic-link-state-loading.md`; `_bmad-output/implementation-artifacts/deferred-work.md`; `_bmad-output/implementation-artifacts/spec-3-6-implement-eventstore-backed-magic-link-state-loading.md`; `_bmad-output/implementation-artifacts/spec-3-6-live-host-catalog-freshness.md`; `_bmad-output/implementation-artifacts/spec-3-6-loader-fold-integrity.md`; `_bmad-output/implementation-artifacts/spec-3-6-projection-write-and-fold-correctness.md`; `_bmad-output/implementation-artifacts/spec-3-6-review-hardening.md`; `_bmad-output/implementation-artifacts/spec-3-6-implement-eventstore-backed-magic-link-state-loading-2.md`; `_bmad-output/implementation-artifacts/spec-3-6-implement-eventstore-backed-magic-link-state-loading-3.md`; `_bmad-output/implementation-artifacts/sprint-status.yaml`; `_bmad-output/implementation-artifacts/tests/3-6-test-summary.md`.
- Root-declared submodule pointers intentionally carried by the accepted Story 3.6 history: `references/Hexalith.Builds`; `references/Hexalith.Conversations`; `references/Hexalith.EventStore`; `references/Hexalith.FrontComposer`; `references/Hexalith.Tenants`; `references/Hexalith.Works`.

## Change Log

| Date | Change |
|------|--------|
| 2026-09-17 | Closed the accepted review-evidence gaps: documented server-derived correction scope and legacy fallback intent, pinned issuance and scope-lineage regressions including serialized approved/adjusted replay, hardened HTTP/readiness helpers, reconciled release ownership guidance and file tracking, and verified 945 tests (941 pass, 4 declared skips, 0 failures) under SDK `10.0.401`. Status remains in-progress pending review. |
| 2026-06-22 | Implemented EventStore-backed `IMagicLinkConfirmationCapabilityStateLoader` + rebuildable token-hash→capability index + trusted-context accessor; replaced the `Unavailable` DI default; added 11 loader unit tests (resolve/fold, idempotency-determinism, per-stage no-disclosure fail-closed, non-authoritative index, admin tenant) and a diagnostics-privacy fitness test. Build `-warnaserror` clean; affected lanes green (651 passed / 3 pre-existing skips). Status → review. |
| 2026-06-22 | Senior Developer Review (AI, autonomous auto-fix). No CRITICAL/HIGH findings. Fixed 1 MEDIUM (non-deterministic catalog `AsOfUtc = DateTimeOffset.UtcNow` → `null`, restoring AC2 catalog determinism and matching the canonical projection) and 1 LOW (removed dead `TryAddScoped<UnavailableMagicLinkConfirmationCapabilityStateLoader>()` DI registration). Build `-warnaserror` clean; affected lanes re-run green (Server 403, Architecture 27, Contracts 86, Projections 77). Status → done. |

## Senior Developer Review (AI)

**Reviewer:** Jérôme Piquot (autonomous story-automator review) · **Date:** 2026-06-22 · **Outcome:** Approve (auto-fix applied)

Adversarial review of the full File List against the four ACs and the git working tree. Build is `-warnaserror` clean (0/0) and every affected test lane is green. All tasks marked `[x]` have real, test-backed implementations; the authority split (non-authoritative index candidate resolver vs. folded authoritative aggregate state), per-stage opaque fail-closed behavior, and dedup-by-message-id + order-by-sequence idempotency are correctly implemented and proven by tests. The git working tree matches the File List (the only out-of-File-List working-tree change, `docs/boundary-decision-record.md`, belongs to Story 1.10, not this story).

**Findings & resolution:**

- 🟡 **MEDIUM — Non-deterministic catalog freshness (AC2).** `LoadActivityTypeCatalogAsync` set `ProjectionFreshnessMetadata.AsOfUtc = DateTimeOffset.UtcNow`, so two folds of the identical event set produced different catalog "trust state", and it reached for the system clock directly (the canonical `TenantActivityTypeCatalogProjection` passes `null` for a Fresh read; the sequence cursor already conveys freshness). **Fixed:** pass `null` for `AsOfUtc`. (`EventStoreMagicLinkConfirmationCapabilityStateLoader.cs`)
- 🟢 **LOW — Dead DI registration.** `ServiceCollectionExtensions` registered `TryAddScoped<UnavailableMagicLinkConfirmationCapabilityStateLoader>()` as its own concrete type, which nothing resolves from the container (the loader interface maps to the EventStore implementation; the only test constructs the stub with `new()`). **Fixed:** removed the dead registration. (`ServiceCollectionExtensions.cs`)

**Verified observations carried forward (no change — consistent with documented deferral, owned by the runtime/Story 3.7 line):**

- The token-hash index ships as a pure rebuildable fold (`Rebuild`/`Apply`) + `IReadModelStore` read seam but has no live `IDomainProjectionHandler` wiring that populates the read model in the running topology — so the live host still resolves an empty index until that wiring lands. This is explicitly deferred in the Completion Notes and matches every other Timesheets projection, which today ships its pure fold + read seam only.
- The loader deserializes event payloads with `JsonSerializerDefaults.Web` and matches event types by simple/`EndsWith` name, ignoring `SerializationFormat`; tests are self-consistent (same options round-trip), so production payload-format fidelity should be confirmed when the projection-host wiring is built. The reviewed value objects (`MagicLinkTokenHash`, `TenantReference`, …) round-trip correctly under STJ Web.
- Performance: the catalog fold and admin reads use a domain-wide (`aggregateId: null`) replay, and the index is a single global read-model document; acceptable for the v1 candidate-resolver but worth revisiting under runtime load.

## 2026-09-16 Supersession and Policy Clarification

This notice supersedes the stale implementation observations above without rewriting the historical review record:

- The canonical token-hash index and tenant Activity Type catalog now have live projection handlers discovered by the host; the four confirm/adjust HTTP routes are exercised after projection delivery without direct read-model seeding.
- Event decoding now accepts only the exact full or simple supported event type name and JSON serialization format; suffix matching and format-agnostic deserialization are no longer used.
- Activity Type loading reads the persisted canonical tenant catalog projection rather than replaying Activity Type domain streams in the loader. Capability and Time Entry streams remain authoritative folds; the token index remains only a candidate locator.
- Existing-link bundles require matching capability/Time Entry Activity Type IDs, folded tenant scope, and exactly one current tenant-owned catalog item. Issue and adjustment selection enforce the same tenant-only ownership policy while Project and Work targets remain supported.
- `TimeEntryCorrectionValues` now carries an optional, null-omitted, non-positional server-derived scope. New correction and adjustment writers record it; aggregate, evidence projection, and ledger folds preserve it. Legacy scope-less corrections retain the preceding scope deterministically and are never inferred from the current catalog.

The original loader story is ready for review together with the 2026-09-16 remediation specification and synchronized sprint tracking.

## 2026-09-17 Review-Evidence Verification

The follow-up review-evidence increment pins correction-scope contract ownership, legacy replay semantics, issuance rejection identity, nested adjustment scope agreement, serialized approved/adjusted replay, and mismatched rejected-correction retry behavior. Launch-readiness ownership and release evidence now distinguish the folded Time Entry ownership gate from defensive catalog checks and record the routine correction/reissue risk. The final post-review inventory under SDK `10.0.401` is 945 tests total, 941 passed, 4 declared infrastructure/performance skips, and 0 failures across all six test projects. Story and sprint status remain `in-progress` pending review.

### Review Findings — independent review of review-evidence increment (2026-09-17)

Scope reviewed: `b0f4ee1...HEAD` (20 files, +493/-82), the increment specified by `spec-3-6-implement-eventstore-backed-magic-link-state-loading-3.md`. Four independent layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor.

**Decision needed**

- [ ] [Review][Decision] This increment moves sibling submodule pointers that spec-3 forbids — `references/Hexalith.Builds` `000abf8`→`04d9617`, `Hexalith.FrontComposer` `1e9348e`→`f20a1fc`, and `Hexalith.Works` `06d64b0`→`3c042f9` sit inside `b0f4ee1...HEAD`. Spec-3 Never bans topology/UI/package/sibling changes; spec-3 BH-12 rejected the claim on the grounds that no new `references/` movement was introduced; the Story 3.6 File List now lists those pointers as owned inventory. Options: (a) revert the three gitlinks and restatement of catalog Aspire `13.5.4` / Keycloak preview evidence against the baseline catalog; (b) keep them and record this increment as owning the pointer moves, as an explicit exception to Never. [references/Hexalith.Builds]
- [ ] [Review][Decision] Story and sprint lifecycle still disagree after the increment that was supposed to reconcile them — the story file stays `Status: in-progress` and the 2026-09-17 verification paragraph says story *and* sprint remain `in-progress`; `sprint-status.yaml` is `review`; fitness tests require “Story 3.6 remains in progress”. Spec-3 BH-02 said both stay `in-progress` until this independent review succeeds, while BMad uses `review` to mean ready for that review. Options: (a) restore sprint to `in-progress` until presentation; (b) align the story file and verification notes to `review`; (c) keep the split as the intended workflow state. [_bmad-output/implementation-artifacts/sprint-status.yaml]

**Patch**

- [ ] [Review][Patch] Deferred-work VG-02 still says the dated package inventory claims `Microsoft.NET.Test.Sdk` `18.10.0`, but this increment already wrote `18.10.1` into that inventory [_bmad-output/implementation-artifacts/deferred-work.md:243]
- [ ] [Review][Patch] Package-currency and Build-gate prose still present the 2026-09-15 `aspire start` / `security` Healthy / `aspire stop` smoke as current after `references/Hexalith.Builds` moved Aspire/Keycloak to `13.5.4`, and `Launch_readiness_record_captures_package_currency_verdict_dimensions` requires those strings, so the stale runtime claim cannot be dated or removed without reddening ArchitectureTests [docs/launch-readiness.md:14]
- [ ] [Review][Patch] The catalog-freshness-round patch that `docs/launch-readiness.md` still describes live magic-link resolution as unwired remains unchecked, although this increment rewrote that classification row to `implemented / waived` with stub caveats [_bmad-output/implementation-artifacts/3-6-implement-eventstore-backed-magic-link-state-loading.md:133]
- [ ] [Review][Patch] The “agent-context files still say the token-hash index is unwired” item is ledgered twice — the 2026-09-16 spec-2 row and the new 2026-09-17 BH-13 row — instead of pointing at the existing open entry [_bmad-output/implementation-artifacts/deferred-work.md:230]

**Deferred**

- [x] [Review][Defer] AppHost Keycloak/Aspire catalog bump is untested at runtime [docs/launch-readiness.md:22] — deferred: pre-existing infrastructure gap already ledgered by this increment; closing it needs an AppHost runtime smoke lane, which is package/topology work this spec must not absorb.
- [x] [Review][Defer] Nested adjustment-scope disagreement is rejected only inside `LoadTimeEntryAsync`; `TimeEntryState.Apply` and `TimeEntryEvidenceProjection.Apply` still fold a top-level `Tenant` with nested `Project` into published evidence [src/Hexalith.Timesheets.Server/TimeEntries/TimeEntryState.cs:145] — deferred: pre-existing fold behavior; spec-3 Code Map forbids changing those folds, and the increment’s loader guard is the specified control.
- [x] [Review][Defer] `LoadTimeEntryAsync` applies `TimeEntryCorrected` / `TimeEntryApprovedCorrected` when `PreviousValues.ActivityTypeScope` disagrees with folded state [src/Hexalith.Timesheets.Server/MagicLinks/EventStoreMagicLinkConfirmationCapabilityStateLoader.cs:293] — deferred: pre-existing; sanctioned writers emit both snapshots or omit both; spec-2 rejected adding this fold guard as malformed-history-only.

**Rejected**

- `false` — [blind-hunter] `LaunchReadinessTests` hard-pins `FAIL` and “Story 3.6 remains in progress”: that is the required honest-verdict control, not a trap. Those assertions should fail when that posture changes, which is when the readiness record must be rewritten anyway.
- `false` — [blind-hunter] `LoadTokenStateAsyncRejectsSerializedAdjustmentScopeDisagreement` never exercises legacy-null `PreviousValues` with mismatched `AdjustedValues`: `Adjusted()` always sets top-level scope to `Tenant`, and the second disjunct already throws when nested `AdjustedValues.ActivityTypeScope` disagrees with that top-level value, including when previous scope is null.
- `false` — [blind-hunter] mixed AppHost SDK `13.5.3` vs catalog Aspire `13.5.4` is unexplained: `docs/launch-readiness.md` already states the AppHost SDK pin and the imported catalog versions as distinct restored facts, and fitness tests pin both from source.
- `false` — [blind-hunter] Stories 3.3 and 3.4 remain `done` while durable POST persistence is FAIL: spec-3 explicitly split that work into a dedicated high-priority remediation and forbade implying substituted HTTP journeys prove persisted writes; the ledger entry exists.
- `false` — [blind-hunter] OpenAPI `readOnly` on `activityTypeScope` is incomplete because `TimesheetsMetadataCatalog` still describes `TimeEntryCorrectionValues` as submitted `correctedValues`: correction commands do not accept that type or `ActivityTypeScope`; spec-3 AC required the OpenAPI field to be read-only, documented, and absent from required input, which is what landed.
- rejected per rule — [blind-hunter] spec Code Map says loader comments-only while the increment adds a throwing fold-integrity check: the specified BH-03/EC-01 patch required that guard; the proposed fix is to edit the spec under review.
- `low` — [blind-hunter] `ProjectOwnedActivityTypeIsRejectedAcrossIssueDisplayConfirmAndAdjust` mixes Work issuance with Project display/confirm and a tenant-scoped adjust entry: issuance identity is the required proof and is pinned; the other three arms still reject, and spec-3 BH-05 already rejected a second identical Project-target rejection test.
- `low` — [blind-hunter] `ApprovedTimeLedgerRowReadModel.SupersededFromApprovedCorrection` still uses `priorValues.ActivityTypeScope ?? evidence.ActivityTypeScope` without the new comment: spec-3 named `TimeEntryState` and `TimeEntryEvidenceProjection` for that documentation; the ledger fallback is unchanged and the same policy is already written on those writers.
- `low` — [blind-hunter] `LoadTokenStateAsyncRejectsSerializedAdjustmentScopeDisagreement` is a `[Fact]` with a `foreach`: both disagreement pairs still run when the first passes; converting to `[Theory]` is hygiene, not a missed defect.
