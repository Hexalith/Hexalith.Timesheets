# Test Automation Summary — Story 4.10 (NFR11 Report/Export/Dashboard Performance Evidence)

Generated by the `bmad-qa-generate-e2e-tests` workflow on 2026-06-22 for `Jerome`.
Role: QA automation engineer (test generation only — no code review / story re-validation).

## Scope

Story 4.10 ships the **opt-in** NFR11 performance lane (`ReportExportDashboardQueryPerformanceLaneTests`,
gated behind `TIMESHEETS_PERF=1`). Its rich correctness assertions therefore only execute when the lane is
opted in — they never run in the default CI baseline. This is a backend read-path story with **no UI surface**,
so "E2E/API tests" map to the project's service-level composed-read suites.

Framework detected and reused: **xUnit v3 (3.2.2) · Shouldly (4.3.0)** (`net10.0`). No new framework, package,
env var, or perf project introduced; `PerformanceStatistics` and reserved placeholders left untouched.
Test command (VSTest socket is blocked in the sandbox — run the built xUnit v3 executable directly):
`./tests/Hexalith.Timesheets.IntegrationTests/bin/Debug/net10.0/Hexalith.Timesheets.IntegrationTests`.

## Gaps discovered and auto-applied

| # | Gap (AC) | Risk before | Test(s) added |
|---|----------|-------------|---------------|
| 1 | **AC5 / NFR12 — privacy-safe emission** had **zero** automated guard (only a doc paragraph + a code comment). | A future edit could leak report/ledger rows, CSV, comments, or identifiers into the evidence output / doc with nothing failing. | `PerformanceEvidenceTests.Report_export_dashboard_lane_emits_privacy_safe_nfr11_aggregates_only` |
| 2 | **AC1/AC2 — composed read journey** correctness only ran behind `TIMESHEETS_PERF=1`; the default baseline never exercised the report→ledger→export→preview→dashboard composition, and **no fail-closed (denied authorization) case** existed at the composition level. | A regression in any composed read path, or in the fail-closed boundary, passes the default suite and only surfaces on a manual opt-in run. | `ReportExportDashboardReadJourneyE2ETests` (6 facts) + `PerformanceEvidenceTests.Report_export_dashboard_read_journey_has_fast_baseline_functional_coverage` |

## Generated tests

### E2E / API — service-level, fast baseline (runs by default, no opt-in)

`tests/Hexalith.Timesheets.IntegrationTests/ReportExportDashboardReadJourneyE2ETests.cs`

- [x] `Project_report_discloses_expected_rows_across_tenant_project_and_period_filters` — happy path, AC1 (tenant / +project / +period / +project+period; rows 6/2/3/1, Fresh).
- [x] `Work_report_discloses_supplied_works_planned_effort` — AC2 Works planned-effort `Supplied` path.
- [x] `Approved_time_ledger_discloses_export_ready_multi_page_source` — AC2 ledger (25-row page over 60 rows, next cursor, export-ready, Fresh).
- [x] `Approved_time_export_generates_full_cursor_scope_while_preview_stays_side_effect_free` — AC2 export (full 60-row scope + CSV) and the 4.9 preview delta (no audit timestamp / no output hash).
- [x] `Dashboard_overview_composes_all_read_sections` — AC2 dashboard fan-out (period/approval/ledger/report sections all Fresh).
- [x] `Report_query_fails_closed_when_tenant_projection_read_is_denied` — **critical error case (NFR8)**: denied `ProjectionRead` ⇒ not disclosed, page null.

### Fitness guards — architecture, fast baseline

`tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/PerformanceEvidenceTests.cs`

- [x] `Report_export_dashboard_lane_emits_privacy_safe_nfr11_aggregates_only` — AC5/NFR12 privacy guard (doc no-PII commitment + lane aggregate-only emission header).
- [x] `Report_export_dashboard_read_journey_has_fast_baseline_functional_coverage` — AC3 guard that fast-baseline functional coverage cannot be silently deleted.

## Coverage

- **NFR11 read paths** (report, work-report, ledger, export, preview, dashboard): measured under the opt-in lane **and now functionally asserted in the default baseline** — 6/6.
- **Acceptance criteria with automated coverage after this run:** AC1 ✅, AC2 ✅, AC3 ✅ (lane skips by default + fast-baseline guard), AC4 ✅ (existing verdict/waiver fitness guards), AC5 ✅ (**newly added**).
- **Error/edge cases:** fail-closed denied-authorization on the report read path (NFR8) — previously uncovered at the composition level.

## Verification (exact counts from built xUnit v3 executables)

| Suite | Before | After | Delta | Result |
|-------|-------:|------:|-------|--------|
| `Hexalith.Timesheets.IntegrationTests` | 81 (77 pass + 4 skip) | **87 (83 pass + 4 skip)** | +6 passing E2E facts | ✅ 0 failed |
| `Hexalith.Timesheets.ArchitectureTests` | 31 | **33** | +2 fitness facts | ✅ 0 failed |

- Solution build: `0 Warning(s), 0 Error(s)` (`-warnaserror`, `-m:1 /nr:false`).
- The NFR11 opt-in perf lane **still skips by default** (4 skips unchanged: NFR10 lane, NFR11 lane, 2 reserved placeholders) — the fast baseline stays fast and stable (AC3).
- Tests are independent (each builds its own fixture via a static factory; no shared mutable state, no order dependency), use semantic Shouldly assertions, and contain no hardcoded waits/sleeps.

## Files changed

- `tests/Hexalith.Timesheets.IntegrationTests/ReportExportDashboardReadJourneyE2ETests.cs` (new)
- `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/PerformanceEvidenceTests.cs` (+2 fitness facts)
- `_bmad-output/implementation-artifacts/tests/4-10-test-summary.md` (this summary)

## Next steps

- Refresh recorded p95 numbers when hardware/runtime changes:
  `TIMESHEETS_PERF=1 … -class "Hexalith.Timesheets.IntegrationTests.ReportExportDashboardQueryPerformanceLaneTests"`.
- EventStore-backed wire path remains `waived (deferred)` (Story 4.10 owns; Epic 5 aggregates) — intentionally no test added.
