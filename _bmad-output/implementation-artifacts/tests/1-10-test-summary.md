# Test Automation Summary — Story 1.10 (Work Reference Validation Adapter)

Generated by the `bmad-qa-generate-e2e-tests` workflow on 2026-06-22 for
`WorksQueryWorkReferenceValidator` and its host composition.

> Written as a story-scoped file so the existing `test-summary.md` (Story 4.7 dashboard) is preserved.

## Framework

xUnit v3 + Shouldly + NSubstitute (project standard). No UI/HTTP surface exists for this
adapter, so the "E2E" tier is the **composed-service** test: a DI-resolved
`TimesheetsAccessGuard` invokes the real adapter over the real `QueryEnvelope`/`QueryResult`
wire contract (Epic 1 retro requirement — composed proof, not source-scan-only).

## Generated Tests (gaps auto-applied)

### Composed-service / E2E — `tests/Hexalith.Timesheets.Works.Tests/WorksReferenceValidationCompositionTests.cs`
- [x] `ComposedGuardMapsFoundWorkStatusToExpectedOutcome` (Theory, 6 rows) — proves each Works
  status survives the guard's `MapReferenceState` end-to-end through the real adapter:
  `Completed → allow / None`, `Suspended|Cancelled|Rejected|Expired → deny /
  UnavailableSiblingAuthority`, `Unknown → deny / AmbiguousAuthority`.
- [x] `ComposedGuardMapsCrossTenantWorkToCrossTenantTarget` — `TenantMismatch → CrossTenantTarget`.
- [x] `ComposedGuardMapsTransportFailureToUnavailableSiblingAuthority` — `Unavailable →
  UnavailableSiblingAuthority`.

### Unit edge cases — `tests/Hexalith.Timesheets.Works.Tests/WorksQueryWorkReferenceValidatorEdgeCaseTests.cs` (new)
- [x] `EnvelopeUsesActorPartyIdAsUserIdAndPropagatesCorrelationId` — envelope audit propagation.
- [x] `EnvelopeFallsBackToCorrelationIdAsUserIdWhenActorIsMissing` — null-actor fallback branch.
- [x] `TenantMatchIsCaseInsensitiveSoMixedCaseRequestTenantStillValidates` — guards the
  `ToLowerInvariant()` tenant compare (mixed-case request tenant must not false-positive
  `TenantMismatch`).
- [x] `ConstructorRejectsNullChannel` / `ValidateAsyncRejectsNullContext` /
  `ValidateAsyncRejectsNullWork` — fail-fast argument contract.

## Why these gaps

The existing 25 tests proved `status → ReferenceValidationState` and a happy/NotFound/DenyAll
composition slice. The auto-applied gaps close: (A) the composed-service **state → denial-category**
mapping for every non-`InvalidReference` outcome and the `Completed → allow` policy decision;
(B) the untested `BuildEnvelope` userId fallback branch; (C) case-insensitive tenant matching;
(D) the argument-null contract.

## Coverage

- Works status → state mapping: 10/10 statuses (unit) — pre-existing, retained.
- Adapter outcome → `TimesheetsDenialCategory` through the composed guard: **8/8** reachable
  outcomes (allow, InvalidReference, CrossTenantTarget, AmbiguousAuthority,
  UnavailableSiblingAuthority from disabled-status and from transport-failure, plus DenyAll
  fallback) — was 2/8.
- `BuildEnvelope` branches: actor-present + actor-fallback — was 0/2 (only field shape checked).
- Argument guards: 3/3 — was 0/3.
- AC4 enumerated states (available, stale, unavailable, unauthorized, cross-tenant, ambiguous,
  disabled, missing, duplicate, replayed): all covered; `stale`/`unauthorized` are documented as
  never positively emitted by this adapter (behavior note Q3) and fail closed to
  `Unavailable`/deny.

## Results

```
Hexalith.Timesheets.Works.Tests  Total: 39, Failed: 0, Skipped: 0   (25 existing + 14 new)
```

- Build: `dotnet build tests/Hexalith.Timesheets.Works.Tests -warnaserror -m:1` → 0 warnings, 0 errors.
- Run: built xUnit v3 executable invoked directly (VSTest socket blocked in sandbox, per Stories
  1.1–1.9): `./tests/Hexalith.Timesheets.Works.Tests/bin/Debug/net10.0/Hexalith.Timesheets.Works.Tests`.

## Next Steps

- Run in CI alongside the other Timesheets suites.
- If the time-capture policy owner overturns the Q2 defaults (`Completed → Valid`,
  `Suspended → DisabledOrArchived`), update the `[InlineData]` rows in
  `ComposedGuardMapsFoundWorkStatusToExpectedOutcome` and the unit mapping theory together.

## Checklist Result

- [x] API/service tests generated where applicable (composed-service is the API-equivalent tier).
- [x] E2E/composition tests generated for the adapter authorization workflow.
- [x] Tests use standard xUnit v3, Shouldly, NSubstitute, and existing project patterns.
- [x] Tests cover happy path plus critical fail-closed cases (disabled, ambiguous, cross-tenant,
  transport-unavailable) and argument/edge contracts.
- [x] Tests use semantic, accessible assertions (typed states/categories); no browser UI exists.
- [x] Tests have clear descriptions and no hardcoded waits or sleeps.
- [x] Tests are independent (fresh substitutes/providers per test) and pass via the fallback runner.
- [x] Summary includes coverage metrics and validation evidence.
