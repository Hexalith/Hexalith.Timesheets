# Launch Readiness Record

Timesheets is story-complete for the Epic 1-4 feature scope, but it is not launch-complete until the explicit launch-scope waivers below are resolved or formally accepted. This record classifies committed evidence at baseline `b0c41aaa3ad899ca13d49cd66e4f26f3b8e3ae5f` on 2026-06-26 and distinguishes concrete implemented behavior from unavailable defaults, skipped lanes, deferred integrations, package-currency evidence, and post-v1 decisions.

Related evidence:

- `docs/performance-evidence.md` records NFR10/NFR11 measured in-process performance evidence and the deferred EventStore-backed wire-path waiver.
- `docs/boundary-decision-record.md` records the owns-vs-references boundary and the fail-closed sibling-module integration defaults.

Story 5.3 review-patch evidence was rerun on 2026-09-12 from the worktree based on `5b56d289c79fa040a306a0db700e9d1f42733baf`, using SDK `10.0.400` and the approved expanded submodule-pin scope: Builds `a32cb422749352cce8dec948aa3e78c8f00eb4cf`, Conversations `73bcee6f04479d4743d5a65ce929728e22687d7d`, EventStore `6b0247acc0b3ef60eb00c0f9ac9cbd367f85da20`, FrontComposer `1b3608c9b039dbba1be0884d92a2a6d54054e370`, Projects `4f05a352edd67c4d5595913ee584539c1948dd58`, and Tenants `2fac18396ff11a4459de053b3ebb7ddfe7c13e30`. Works remained pinned at `42c43180df201883512369c680db1d091f57b02d`.

## Package-Currency Verdict

Current package-currency verdict (Story 5.2 reopened evidence, 2026-09-12): **CONCERNS**. The Timesheets-owned baseline is aligned at .NET SDK `10.0.401` (`rollForward: latestPatch`) and `Aspire.AppHost.Sdk` `13.5.3`; stable direct packages are current, root npm work is not applicable, and vulnerable/deprecated audits are clean. The solution/AppHost transitive-outdated tooling failure remains visible, all other project fallbacks were reviewed with no pin, and the platform-owned prereleases observed in the Timesheets restored packages plus the Fluent UI V5 policy surface remain an explicit waiver rather than a hidden stable-package claim. Approval evidence is the approved 2026-09-12 course correction in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-12.md` §7. Review-time AppHost smoke evidence also passed: `aspire start` completed, the `security` resource reported `Healthy`, and `aspire stop` completed successfully. Re-verified on 2026-09-15 after the AppHost gained the Timesheets project and its public/internal endpoint split: every resource reported `Healthy` and `aspire stop` completed cleanly.

| Dimension | Verdict | Evidence | Rationale |
|---|---|---|---|
| Direct Timesheets NuGet package currency | PASS | After a forced/no-cache SDK `10.0.401` restore, `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet list Hexalith.Timesheets.slnx package --outdated --no-restore` reported no direct package updates for all 15 Timesheets projects. Resolved direct versions are `MinVer` `8.0.0`; ASP.NET Core MVC/DI `10.0.12`; Extensions resilience/service discovery `10.10.0`; OpenTelemetry `1.18.0`; `coverlet.collector` `10.0.1`; `Microsoft.NET.Test.Sdk` `18.10.0`; `NSubstitute` `6.2.0`; `Shouldly` `4.3.0`; and xUnit `4.0.0`. AppHost-generated direct assets resolve at `13.5.3`. | `Directory.Packages.props` still imports the centrally managed `Hexalith.Builds` catalog; project `PackageReference` items remain versionless, the AppHost SDK is the only Timesheets-owned project SDK pin, and no `.sln` was introduced. |
| Vulnerable and deprecated packages | PASS | `dotnet list Hexalith.Timesheets.slnx package --vulnerable --include-transitive --no-restore` and `dotnet list Hexalith.Timesheets.slnx package --deprecated --include-transitive --no-restore` both covered all 15 projects and reported no vulnerable or deprecated packages from `https://api.nuget.org/v3/index.json`. | No compatibility, security, or deterministic-build reason was found for adding an explicit transitive pin. |
| Root npm applicability | not applicable | The Timesheets root has no `package.json`, `package-lock.json`, `npm-shrinkwrap.json`, `pnpm-lock.yaml`, or `yarn.lock`. | Manifests under `Hexalith.*` are sibling-submodule owned and excluded from this Timesheets root audit. |
| Transitive drift | reviewed, no pin / tooling unavailable | Solution-level `dotnet list Hexalith.Timesheets.slnx package --outdated --include-transitive --no-restore` and the AppHost project fallback both failed exactly with `error: Sequence contains no matching element`; this lane is not reported clean. The other 14 project fallbacks succeeded. Reported drift is `Google.Protobuf` `3.35.0 -> 3.36.1` and gRPC `2.80.0 -> 2.83.0` through sibling EventStore/Dapr; Polly `8.4.2 -> 8.7.0` through direct `Microsoft.Extensions.Http.Resilience`; Shouldly helpers (`DiffEngine`, `EmptyFiles`, `System.CodeDom`, `System.Management`); xUnit/Microsoft.Testing dependencies; and NSubstitute dependencies (`Castle.Core`, `System.Diagnostics.EventLog`). | The drift is platform-transitive or test-stack only, and vulnerability/deprecation audits are clean. Adding a root transitive pin would promote package dependencies without a compatibility, security, or deterministic-build reason. |
| Platform and submodule alignment | waived | Root Timesheets pins SDK `10.0.401` and AppHost SDK `13.5.3`; the imported catalog aligns Aspire packages at stable `13.5.3` and the Dapr family at stable `1.18.7`. Timesheets has no direct Dapr or Fluent UI package. The Timesheets restored AppHost graph includes the platform-owned prereleases `Aspire.Hosting.Keycloak` `13.5.3-preview.1.26425.3` and `CommunityToolkit.Aspire.Hosting.Dapr` `13.5.1-beta.752` through `Hexalith.EventStore.Aspire`; the relevant Fluent UI V5 policy-surface catalog entry is `5.0.0-rc.5-26219.1`. This is scoped Timesheets evidence, not a complete shared-catalog prerelease inventory. `references/Hexalith.Builds/Props/Directory.Packages.props` remains submodule-owned. | Owner: Platform / Hexalith.Builds and Hexalith.EventStore. Approval: the approved 2026-09-12 course correction in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-12.md` §7. Risk: the AppHost depends on prerelease platform integrations and documentation could misstate catalog prereleases as stable Timesheets direct pins. Revisit condition: the platform publishes and validates stable Keycloak/Dapr integration and Fluent UI V5 catalog lines, or explicitly approves their continued prerelease use for release. |

## Works Checkout Ownership

This Timesheets repository is the umbrella pin for Works. The sole default checkout is `references/Hexalith.Works` (`<workspace>/references/Hexalith.Works`). A repository-root `Hexalith.Works` gitlink, `.gitmodules` path, or `$(MSBuildThisFileDirectory)Hexalith.Works` probe is forbidden. Do not initialize `Hexalith.Timesheets/Hexalith.Works`. A caller-supplied `HexalithWorksRoot` is preserved as read-only input; otherwise MSBuild resolves `references/Hexalith.Works` first and sibling `../Hexalith.Works` second. This section does not claim live Works host integration: default host wiring still denies live Work validation and planned-effort reporting until explicit opt-in.

| Dimension | Verdict | Evidence | Rationale |
|---|---|---|---|
| Default Works checkout | PASS | `.gitmodules` path `references/Hexalith.Works`; gitlink `160000` at that path; `Directory.Build.props` probes `references\Hexalith.Works` then `..\Hexalith.Works`. | This clone owns the umbrella pin. Stories 1.10 and 4.8 consume Works Contracts from that checkout. |
| Repository-root Works gitlink or probe | PASS | `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/WorksCheckoutGovernanceTests.cs` rejects a root `Hexalith.Works` path, gitlink, and `$(MSBuildThisFileDirectory)Hexalith.Works` probe. | No forbidden root checkout is present; a second checkout would silently bind a different Works commit. |
| Live Works host integration | waived | Unchanged Story 1.10 / 4.8 host defaults. | Checkout ownership is not live host wiring. |

## Launch-Scope Classification

Classification vocabulary: atomic labels are `implemented`, `waived`, and `post-v1`; compound labels such as `implemented / waived` mean the implemented control is proven while the named residual risk remains explicitly waived.

| Item | Verified state | Classification (implemented/waived/post-v1) | Owner | Risk | Revisit condition | Evidence |
|---|---|---|---|---|---|---|
| Unavailable defaults | Host keeps fail-closed defaults for live Works validation and planned-effort reporting unless explicit opt-in wiring supplies live channels. | waived | Story 1.10 / Story 4.8 / platform | Work writes are denied and planned effort is unavailable in the default host. | Host binds `IWorksQueryChannel`, calls `AddTimesheetsWorksReferenceValidation()`, and calls `AddTimesheetsWorksPlannedEffortReporting()`. | `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs`; `docs/boundary-decision-record.md` |
| Skipped lanes | Infrastructure and performance placeholders remain static `[Fact(Skip=...)]` reservations for runtime EventStore/Dapr/Aspire evidence. | waived | Data-bearing runtime story | Runtime paths are reserved, not proven by default suites. | Realistic EventStore/Dapr/Aspire fixtures land and the lanes become runnable without slowing the fast baseline. | `tests/Hexalith.Timesheets.IntegrationTests/InfrastructureLaneTests.cs`; `tests/Hexalith.Timesheets.IntegrationTests/PerformanceEvidenceLaneTests.cs` |
| Deferred integrations | Export-preview HTTP routing and live Works host channels are not launch-wired. | waived | Platform / owning feature stories | Existing service behavior can be mistaken for launch-active runtime integration. | Runtime topology wires each remaining deferred integration, or product docs remove the corresponding launch claim. | `src/Hexalith.Timesheets.Server/Exports/ApprovedTimeExportService.cs`; `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs` |
| Legal-hold policy | Default retention policy is implemented and documented; legal-hold override remains a launch gate requiring tenant/legal sign-off. | waived | Story 1.4 / tenant + legal | Ships without legal-hold override configured. | Tenant/legal sign-off confirms the launch policy and configuration. | `_bmad-output/planning-artifacts/prds/prd-timesheets-2026-06-18/prd.md` |
| Comment sensitivity | Comment sensitivity vocabulary and fail-closed default are implemented; detailed classification/redaction policy remains open. | implemented / post-v1 | Story 1.4 / PM | Comment classification rules are unresolved beyond the v1 vocabulary. | PRD question Q8 is decided and a policy-bearing story implements classification rules. | `src/Hexalith.Timesheets.Contracts/openapi/timesheets-evidence-policy.v1.md`; PRD Q8 |
| Export format | Deterministic approved-time CSV v1 is implemented and golden-file tested; structured API/webhook contract remains open. | implemented / post-v1 | Story 4.5 / PM | Downstream billing may need a structured contract beyond evidence CSV. | PRD Q7 selects the launch export integration contract. | `tests/Hexalith.Timesheets.IntegrationTests/ApprovedTimeExportIntegrationTests.cs`; `tests/Hexalith.Timesheets.IntegrationTests/Exports/Golden/approved-time-export-v1-project-work-boundaries.csv` |
| Secondary magic-link identity verification | v1 uses single-use scoped expiring links; secondary identity verification is explicitly deferred. | post-v1 | Story 3.2 / PM | High-value or billable links rely on scoped single-use controls only. | Post-v1 security/product review decides secondary verification. | PRD Q4 |
| Performance evidence | NFR10 and NFR11 in-process paths pass measured p95 targets; EventStore-backed persistence/Dapr wire path is waived. | implemented / waived | Stories 1.11 and 4.10 | In-process p95 does not prove persisted runtime topology latency. | Realistic EventStore/Dapr/Aspire persisted fixtures exist and the wire-path lane runs. | `docs/performance-evidence.md`; `tests/Hexalith.Timesheets.IntegrationTests/*PerformanceLaneTests.cs` |
| Live Works reference validation in host | `WorksQueryWorkReferenceValidator` is concrete, but the default host keeps `DenyAllWorkReferenceValidator` because no live `IWorksQueryChannel` is bound. | waived | Story 1.10 / platform | Live Work capture remains denied in the running host. | Host binds the Works query channel and opts into Works reference validation. | `src/Hexalith.Timesheets.Works/WorksQueryWorkReferenceValidator.cs`; `src/Hexalith.Timesheets.Server/Runtime/ServiceCollectionExtensions.cs` |
| Live Works planned-effort reporting | `WorksQueryWorkPlannedEffortProvider` is concrete and covered by Works tests; the host keeps `UnavailableWorkPlannedEffortProvider`. | waived | Story 4.8 / platform | Planned-vs-actual reports show planned effort as unavailable in the default host. | Host wires the provider over a live Works channel. | `src/Hexalith.Timesheets.Works`; `tests/Hexalith.Timesheets.Works.Tests` |
| Magic-link HTTP no-disclosure | Invalid-link HTTP no-disclosure is implemented and tested through the in-process host. | implemented | Story 3.7 | - | - | `tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs` |
| Magic-link Activity Type ownership inventory | v1 issue, display, confirm, and adjust paths accept tenant-owned Activity Types only while Project and Work remain valid time-entry targets. The repository cannot inspect deployed capability or TimeEntry histories. | implemented / waived | Story 3.6 / release owner | A project-owned capability over normal project-owned Time Entry evidence is reissuable only after an authorized correction changes that Time Entry to a tenant-owned Activity Type, followed by capability revocation and reissuance. A legacy scope-less Project-to-Tenant correction can retain Project scope; a legacy scope-less Tenant-to-Project correction can retain Tenant scope while pointing at a project-owned Activity Type ID absent from the tenant catalog. Both mismatch shapes fail closed and cannot be repaired or reclassified by token reissue alone. | Before rollout, the release owner inventories deployed capability and TimeEntry histories, authorizes correction of repairable project-owned Time Entries to tenant-owned Activity Types, then revokes and reissues their affected capabilities. The owner either explicitly accepts both legacy scope/Activity-Type mismatch shapes or owns a separate append-only remediation for histories that token reissue alone cannot repair. | `src/Hexalith.Timesheets.Server/MagicLinks`; `tests/Hexalith.Timesheets.Server.Tests/MagicLinkConfirmationCapabilityCommandServiceTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs` |
| Magic-link invalid-token timing | All four external routes return equivalent invalid-link status, headers, content type, normalized body, and raw UTF-8 response length. Blank or hash-derivation-rejected tokens perform zero read-model and EventStore I/O; an unknown well-formed hash necessarily performs one token-index lookup. | implemented / waived | Story 3.6 / security | Backend work and distributed dependency timing are not constant across invalid-token classes even though the HTTP representation is opaque. | Revisit before public exposure without suitable abuse controls, if token entropy is weakened, or if evidence shows known invalid tokens can be practically classified by timing. | `tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs`; `tests/Hexalith.Timesheets.Server.Tests/EventStoreMagicLinkConfirmationCapabilityStateLoaderTests.cs` |
| Magic-link live end-to-end resolution | The host discovers the canonical token-hash index and tenant Activity Type catalog projection handlers. After delivery through the mapped projection route, all four valid confirm/adjust HTTP routes pass without direct index or catalog seeding. The proving journey is in-process: it substitutes `IEventStoreGatewayClient`, `IReadModelStore`, and `ITimesheetsAccessGuard`, and `Hexalith.Timesheets.AppHost` declares no `eventstore` resource, so handler discovery and route wiring are proven while live topology is not. | implemented / waived | Stories 3.6 and 3.7 | In-process stubs prove wiring, not persisted EventStore topology or the production access guard. | A persisted EventStore/Dapr fixture exercises the same four routes against a declared `eventstore` resource. | `src/Hexalith.Timesheets/Program.cs`; `src/Hexalith.Timesheets.Projections/MagicLinks/MagicLinkTokenHashCapabilityIndexProjectionHandler.cs`; `src/Hexalith.Timesheets.Projections/ActivityTypes/TenantActivityTypeCatalogProjectionHandler.cs`; `tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs` |
| Export preview | `ApprovedTimeExportService.PreviewAsync` is side-effect-free and tested; no dedicated HTTP route maps the preview capability. | implemented / post-v1 | Story 4.9 | Metadata can imply a callable preview route that does not exist. | A story maps a preview endpoint, or metadata removes the HTTP-route implication. | `src/Hexalith.Timesheets.Server/Exports/ApprovedTimeExportService.cs`; `src/Hexalith.Timesheets.Contracts/TimesheetsMetadataCatalog.cs` |
| Time-zone and period policy | Tenant time zone, UTC audit instants, tenant-local period keys, and DST/boundary export tests are implemented; carry-forward approval behavior remains open. | implemented / post-v1 | Stories 2.7 and 4.6 / PM | Carry-forward handling for existing approved entries remains unresolved. | Period-approval policy decision covers carry-forward behavior. | `tests/Hexalith.Timesheets.IntegrationTests`; PRD Q1 |
| NFR13 UI behavior | No Timesheets UI project exists by design; FrontComposer metadata exists, but component-level accessibility is unproven. | post-v1 | UI-bearing story | WCAG behavior is not proven for Timesheets components. | A UI-bearing story scaffolds `Hexalith.Timesheets.UI` and `Hexalith.Timesheets.UI.Tests`. | `README.md`; current `src/` and `tests/` project list |
| Production deployment evidence | No production deployment artifact exists in this repository. | post-v1 | Release owner | Launch lacks production deployment proof. | Deployment evidence is produced at release/deployment time. | Repository artifacts |
| External stakeholder acceptance | Story-record acceptance exists; no external stakeholder acceptance artifact is present. | post-v1 | PM/PO | Launch lacks external acceptance evidence. | UAT or stakeholder acceptance record is captured. | `_bmad-output/implementation-artifacts` |
| Works staleness detection | `WorkItemView` exposes `LatestAcceptedSourceSequence` but not enough freshness data for the adapter to detect stale Works state fail-closed. | waived | Platform / Works owner | Stale Works state can be accepted as current by the adapter. | Works adds freshness metadata or Timesheets owns an EventStore-backed projection. | Story 1.10 Dev Agent Record; `src/Hexalith.Timesheets.Works` |
| Platform prerelease dependency exception | The Timesheets restored package graph includes Aspire `13.5.3`, Dapr `1.18.7`, Keycloak `13.5.3-preview.1.26425.3`, and CommunityToolkit Aspire Dapr `13.5.1-beta.752`; the relevant Fluent UI V5 policy-surface catalog entry is `5.0.0-rc.5-26219.1`. This is not a complete shared-catalog prerelease inventory. Timesheets directly pins only its AppHost SDK and does not override the platform-owned prereleases. | waived | Platform / Hexalith.Builds and Hexalith.EventStore | Prerelease platform integrations can change behavior before stable release and can be mistaken for approved stable direct Timesheets dependencies. | Platform publishes and validates stable replacements or explicitly approves the prerelease integrations for release. | `Directory.Packages.props`; `references/Hexalith.Builds/Props/Directory.Packages.props`; `_bmad-output/planning-artifacts/architecture.md`; approved 2026-09-12 course correction, `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-12.md` §7 |
| Doc-vs-test-project divergence | Architecture previously listed absent `UnitTests`, `Security.Tests`, `PropertyTests`, `UI`, and `UI.Tests`, and omitted `Works.Tests`. This story corrects the Timesheets-owned test map. | implemented | Story 5.1 | Stale test maps hide actual coverage ownership. | Keep architecture project maps aligned with `src/` and `tests/`. | `_bmad-output/planning-artifacts/architecture.md`; filesystem project list |

## Release-Gate Decision Table

### Control for the domain-service route group

`src/Hexalith.Timesheets/Program.cs` calls `UseEventStoreDomainService()`, which maps the canonical
EventStore SDK routes — `POST /process`, `/replay-state`, `/query`, `/project`, `/project/v2`,
`/project/v2/reconcile`, `/project/rebuild/{v1,stage,commit,abort,verify}`, `/project/rebuild/shared/v1`
and `/admin/operational-index-metadata`. None carries `RequireAuthorization`, and the same host
deliberately serves the anonymous magic-link confirm and adjust routes, so an anonymous
`POST /project/v2` would otherwise write the cross-tenant token-hash index and the trust-bearing
Activity Type catalog, and `/process` would dispatch domain commands.

**Control: an enforced port split.** `InternalSurfaceGuard` runs ahead of routing and refuses the
domain-service path prefixes on any port but the configured internal one, answering `404` rather
than `403` so a refusal does not confirm the route exists. It is fail-closed: with
`Timesheets:InternalSurface:Port` unset the surface is refused everywhere, so a deployment that
forgets to configure the split loses projection delivery instead of publishing a write surface.
`src/Hexalith.Timesheets.AppHost/Program.cs` declares the split — a public `http` endpoint and a
non-proxied `internal` endpoint on port 8081 — and passes that port to the host, which is what makes
this a wired control rather than an assumption about deployment topology.

Evidence: `tests/Hexalith.Timesheets.IntegrationTests/InternalSurfaceGuardTests.cs` proves all nine
protected routes are refused when nothing is configured, and that the public magic-link and metadata
routes stay reachable. `MagicLinkConfirmationHttpBoundaryTests` opts in explicitly via
`AllowOnAnyPort`, because `TestServer` has no listener and reports `Connection.LocalPort` as `0`.

**Live `aspire start` evidence (2026-09-15), against real Kestrel listeners rather than TestServer.**
All resources reported `Healthy` (`aspire-dashboard`, `security`, both `security-*` parameters, and
the `timesheets` project on its `public` and `internal` endpoints); `aspire stop` completed cleanly.
Observed on the public listener (`:8080`): `GET /metadata/timesheets` → `200`; `GET` magic-link
confirm → `403` (the opaque denial, fail-closed with no EventStore resource); `POST /process`,
`/replay-state`, `/query`, `/project`, `/project/v2`, `/admin/operational-index-metadata` → `404`
on every one. On the internal listener (`:8081`): `POST /admin/operational-index-metadata` → `200`
and `POST /project/v2` → `400` (the SDK's own envelope validation), i.e. reachable. The split is
therefore verified end to end and not inferred from the in-process suite.

**Residual risk:** the guard matches by path prefix, so an SDK route added outside those prefixes
would not be covered; and `AllowOnAnyPort` republishes the surface if ever set in a deployed host.
`/health`, `/alive` and `/ready` remain unconditional under the SDK defaults, where the now-uncalled
`MapTimesheetsDefaultEndpoints` gated `/health` behind `IsDevelopment()`.

**Revisit condition:** the infrastructure story that adds the EventStore resource and DAPR sidecars
replaces or reinforces the port split with a DAPR access-control (Configuration CRD) policy via
`AddEventStoreDomainModule(daprConfigPath:)`, which scopes inbound invocation by caller identity
rather than by port.

Verdict vocabulary: `PASS`, `CONCERNS`, `FAIL`, `WAIVED`.

| Gate | Verdict | Evidence | Notes |
|---|---|---|---|
| Build | PASS | `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet restore Hexalith.Timesheets.slnx -m:1 /nr:false --force --no-cache`; `DOTNET_CLI_HOME=/tmp/dotnet-cli-home dotnet build Hexalith.Timesheets.slnx --no-restore -warnaserror -m:1 /nr:false`; review-time `aspire start` / `security` Healthy / `aspire stop` smoke check. | Reopened Story 5.2 evidence on 2026-09-12: forced/no-cache restore and the final warnings-as-errors build passed under SDK `10.0.401` with 0 warnings and 0 errors. Aspire `13.5.3` required `<AspireUseCliBundle>true</AspireUseCliBundle>`; the initial build exposed `ASPIRE010`, and the project opted into the version-aligned CLI bundle, changing orchestration-runtime dependency resolution while leaving declared resource topology and domain/product behavior unchanged. The rerun and review-time runtime smoke check passed. The dated Story 5.3 SDK `10.0.400` evidence above remains historical. |
| Package currency | CONCERNS | Reopened Story 5.2 SDK `10.0.401` restore and package audits on 2026-09-12. | Direct NuGet, vulnerable, and deprecated audits are clean; root npm is not applicable; transitive drift is reviewed with no pin while the solution/AppHost tooling failure remains visible; platform prerelease entries are waived with owner, risk, and revisit condition. |
| Tests (full suite) | PASS | Verified xUnit v3 test inventory on 2026-09-16 under SDK `10.0.401`, after the Story 3.6 loader-policy remediation: ArchitectureTests 55 total / 55 pass; Contracts.Tests 90 / 90 pass; IntegrationTests 104 total / 100 pass / 4 skipped; Projections.Tests 145 / 145 pass; Server.Tests 469 / 469 pass; Works.Tests 76 / 76 pass. | Final total: 939 tests, 935 pass, 4 intentional skips, 0 failures. |
| Privacy/logging scans | PASS | `tests/Hexalith.Timesheets.ArchitectureTests/FitnessTests/DiagnosticsPrivacyTests.cs`; ArchitectureTests 55 / 55 pass. | Covered by the 2026-09-16 ArchitectureTests verification. |
| Projection rebuild/idempotency | PASS | `tests/Hexalith.Timesheets.Projections.Tests/*ProjectionTests.cs`; Projections.Tests 145 / 145 pass, including explicit and legacy correction-scope lineage. | Projection coverage passed with 0 skips. |
| Export golden files | PASS | `tests/Hexalith.Timesheets.IntegrationTests/ApprovedTimeExportIntegrationTests.cs`; `tests/Hexalith.Timesheets.IntegrationTests/Exports/Golden/approved-time-export-v1-project-work-boundaries.csv`; IntegrationTests 104 total / 100 pass / 4 skipped. | Golden-file coverage passed in the default integration executable. |
| Magic-link HTTP no-disclosure | PASS | `tests/Hexalith.Timesheets.IntegrationTests/MagicLinkConfirmationHttpBoundaryTests.cs`; IntegrationTests 104 total / 100 pass / 4 skipped. | Invalid-link safety and canonical-projection delivery for all four valid HTTP routes are implemented; the valid journey asserts zero direct index and catalog seeds, and the concrete loader rejects cross-tenant candidate/aggregate mismatches without protected work. |
| Performance evidence | PASS / WAIVED | `docs/performance-evidence.md`. | In-process NFR10/NFR11 pass; EventStore-backed wire path WAIVED. |
| Tenant-isolation/security | PASS | `tests/Hexalith.Timesheets.Server.Tests/AuthorizationServiceTests.cs`, `AuthorityBoundaryTests.cs`, `FailClosedDefaultsTests.cs`, `TimeEntryAuthorizationTests.cs`; Server.Tests 469 / 469 pass. | Fail-closed defaults, loader integrity, and tenant-only magic-link Activity Type ownership passed. |
| Works checkout ownership | PASS | Story 5.3 fitness tests in `WorksCheckoutGovernanceTests.cs`; `.gitmodules` and git index pin `references/Hexalith.Works` only. | Default checkout is the umbrella `references/` pin; a root gitlink is forbidden. This gate does not claim live Works host integration. |

Overall release decision: **CONCERNS**. The feature scope is story-complete and the evidence is traceable, but launch is not a clean PASS because package/platform alignment, the deployed magic-link Activity Type ownership inventory, invalid-token timing, and live-topology end-to-end resolution still have explicit waivers, while live Works host activation, export-preview HTTP routing, EventStore-backed wire-path performance, NFR13 UI evidence, production deployment evidence, external stakeholder acceptance, legal-hold sign-off, and Works staleness detection are waived or post-v1.
